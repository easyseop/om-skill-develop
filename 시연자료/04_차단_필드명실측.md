> 수집: 2026-08-25, 서버 세션 보고 원문 발췌 — 지시서 오류를 스키마 실측으로 잡은 실례

지시하신 refs.current_custom / refs.official은 initial 모드 필드입니다. 스키마 실측상 mode: upgrade는 refs.official_base·official_target·custom_baseline과 hop_policy·versions·deployment_method·official_documents를 필수로 요구합니다(schema:allOf). 지정 SHA 2개를 official_target/custom_baseline에 매핑하고, 지시받은 이름도 그대로 병기했습니다.
official_base는 지시에 없어 제품 태그 upstream-1.13.1 = afcb2d2cd7…를 넣었습니다 — 사람 확인 필요.
deployment_method, official_documents.source는 임의 생성하지 않고 TODO로 두었습니다.
다음 단계 사전 경고: plan start는 두 저장소 모두 untracked 포함 clean을 요구합니다(preflight.py:283-292). 현재 제품 저장소의 untracked md 1건 때문에 그대로 실행하면 WORKTREE_DIRTY로 막힙니다.

(검증자 확인: official_base=장부 patch_sha와 일치 → 승인. 문서 항목 필드도 검증자 Mac 드라이런에서 추가 교정 — path/title이 아니라 source/version_token. untracked md는 삭제 아닌 저장소 밖 이동으로 해소)
