> 수집: 2026-09-02. 유형: 발견(P0+P1) — 사상 첫 계약 테스트 실행의 수확.
결과 한 줄: 계약 9종 첫 실행 8 fail/1 pass(한글 IME 통과) — 그러나 제품 결함 0. 원인 ①(6건) base_url을 엔진은 서버루트로, 계약 테스트는 API루트로 소비 — 어떤 값도 한쪽을 깨는 규약 모순(이 6종은 "실패"가 아니라 **미검증**). ②(2건) 계약 테스트가 1.13.2에서 사라진 switch 형태(DatabaseServiceUtils.tsx)를 기대 — 기능은 U8 이식대로 PureUtils 맵에 실재. **최초 랩 발견("코드는 이사 갔는데 검사기가 옛 주소만 봄")과 수미상관** — 이번엔 계약 테스트 층에서 재발.
규율 작동: 세션이 waiver(면제 통과)를 "검증이 수행되지 않은 것은 알려진 예외가 아니다"로 스스로 배제, 재실행으로 결과 바꾸기 시도 없음, 실패 run 증거 보존.
조치: 핫픽스 102(A: OPENMETADATA_BASE_URL을 엔진이 API루트로 조립 / B: 앵커를 맵 형태로 갱신, 의도 불변) + 사람 결정 기록(24: 검사 기준 정정으로 규정) + 백로그 #18·#19.
