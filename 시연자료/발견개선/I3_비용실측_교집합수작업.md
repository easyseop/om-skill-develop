> 수집: 2026-08-26 (1단계 run 비용 실측, 서버 세션 보고 원문). 유형: 발견·개선 후보 (P1 — 비용).

결과 한 줄: /om-plan 1회 = 약 30분17초·메인 최소 21만 토큰 + 판독 에이전트 56,205토큰. 비용 대부분은 "교집합 수작업"에서 발생 — 개선 방향: 교집합을 검사기가 사실(fact)로 미리 산출.

## 서버 세션 보고 원문
- plan start 2026-08-26T03:10:02Z → 사실 수집 완료 03:10:03Z → plan check 판정 03:40:19Z (30분 17초)
- 독립 문서 판독 에이전트: 463,106ms ≈ 7분43초, 도구 호출 9회, 56,205 토큰 (전체의 약 1/4)
- 메인 스레드: 관측 감소폭 약 21만 토큰(턴 초반 미표시 구간 있어 실제는 더 큼)
- 원인: discovered-facts.json(911KB, 17,736줄)에서 공식 변경 1,702경로 ∩ 등록 112경로를 손으로 산출. 보호 세션 중 훅이 harness/om plan 외 모든 Bash를 차단해 grep·python 한 줄이면 끝날 집합 연산을 Read 구간 훑기로 8~9회 반복.
- 세션 제안: official-upgrade-paths와 registered-customization-paths가 사실로 따로 있고 교집합은 validate.py 내부에서만 계산됨 — 계획 작성자용으로 교집합 사실을 추가하면 재발 방지.
