> 수집: 2026-08-26. 유형: 발견·개선(P1, #10 가족) — plan→apply 늦은 관문 2호.

결과 한 줄: apply 착수가 APPLY_START_ROLE_MISMATCH로 차단 — upgrade는 official_target(공식 1.13.2)에서 시작해야 하는데 실행계획서의 start_commit_sha가 custom_baseline으로 적혀 있었음. **계획서 본문 전략("공식을 기준으로 커스텀 재적용")은 맞고 숫자 필드 하나만 모순** — LLM 오기 + 계획 게이트가 시작 커밋의 역할을 검사하지 않는 배선 공백의 조합. 역할 검사는 apply start에만 존재(workflow.py:409-417).

이중 확인: 봉인(digest)이 승인본 수정 시도를 재차 차단(APPLY_PLAN_ARTIFACT_STALE 구조) — 변조 방지 설계 재실증.

조치: 새 plan run(start_commit_sha만 official_target으로 교정, 액션 33개 소스 실재는 실측 재검증) + 제품 작업 브랜치를 공식 1.13.2 지점에서 신규 분기(사람 결정). 백로그 #10에 "start_commit_sha 역할 검증을 plan 게이트로 전진 배치" 추가.
