> 수집: 2026-08-26. 차단 사례 ③ — 실기동만이 잡는 결함의 실례 (스모크 테스트 존재 이유의 실증)

## 장면
서버에서 첫 `/om-plan` 입력 → 응답 없이 끊김.

## 원인 (검증자 재현 — Mac 훅 시뮬레이션 원문)
① Expansion(marker 없음) → {'decision': 'block', 'reason': 'direct slash expansion requires a session marker from UserPromptSubmit'}
② Submit(원문 프롬프트 가정) → None  (marker 생성)
③ Expansion(marker 생긴 후) → None
→ 훅 설계는 "발급소(Submit) 먼저"를 가정했으나 실제 이벤트 순서는 "검문소(Expansion) 먼저" — 첫 슬래시 호출이 항상 자기 훅에 차단.
(1차 검토의 부수 발견 "실기동 스모크 필수" 항목이 실기동에서 확정된 것)

## 조치와 재검증 (당일)
- Codex 핫픽스(96): Expansion=발급소로 전환 + 1회용 인계 상태(expansion_submit_pending)로 Submit 중복 오인 방지 + 구환경(Expansion 없음) 경로 보존 + 필드명 이중 수용 + 반례 6종
- 검증자 재실행: 원 재현 시나리오 정확히 역전(①통과+발급 ②통과 ③중복차단), 283 테스트 green
- 커밋 04e9982663 → MR !4

## 교훈
문서 6중 감사·정적 검증이 못 잡는 것(실행 환경의 이벤트 순서)은 실기동만 잡는다.
