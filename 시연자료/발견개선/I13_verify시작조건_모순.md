> 수집: 2026-09-02. 유형: 발견(P0 — 엔진 모순 3호). 승인 창구(I9)와 동형의 "단계 간 인터페이스" 공백.
결과 한 줄: 첫 /om-verify 실행이 VERIFY_CHECKER_DIRTY(infra_error, 미검증)로 정지 — 조사 결과 apply의 성공 조건(관리파일 미커밋 갱신)과 verify의 시작 조건(HEAD 동일+clean)이 **서로 배타적**임이 코드 실측으로 확정(workflow.py:172-179). 커밋·유지·원복 세 경로 모두 다른 검사에 막히는 완전 폐색.
규율 작동 2건: ① 세션이 승인된 커밋 지시를 실행 전 정지 — "결과가 실패로 확정된 되돌리기 어려운 작업"임을 코드로 증명하고 보고(지시 불복종이 아니라 상위 원칙 준수) ② infra_error를 실패도 통과도 아닌 "미검증"으로 정확히 취급, 손으로 확인한 6개 조건을 "증거로 인정되지 않는다"고 자진 명시.
처방: 지시서 100 — 완화가 아닌 정밀화("장부 dirty는 apply-result가 봉인한 digest와 바이트 일치할 때만 허용"). 유형 통찰: 엔진 모순 3건(#10③·엔진동일성·이번)이 전부 "단계 경계에서 앞 단계의 산출 상태와 뒤 단계의 전제가 어긋남" — 파이프라인 인터페이스 계약의 기계 검증(단계 간 사전대조)이 근본 방향.
