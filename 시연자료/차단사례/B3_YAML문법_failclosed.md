> 수집: 2026-08-26 (1단계 run). 유형: 차단 — 깨진 입력은 한 발도 못 나감.
결과 한 줄: 세션이 쓴 951줄 계획서의 들여쓰기 오류 1곳(잘못된 위치의 note: 키) → plan check가 INPUT_UNREADABLE·analysis_error(3)로 정지. 부분 수용 없음.
원문(reasons): INPUT_UNREADABLE: cannot read .../proposal/upgrade-1.13.1-to-1.13.2.yaml: while parsing a block collection ... line 791 ... expected <block end>, but found '?' in line 819
후속: marker 정리로 같은 run 재검증 불가(재개는 block 전용) → 새 run 필요. 이 재시도 비용은 발견개선 I2로 등재.
