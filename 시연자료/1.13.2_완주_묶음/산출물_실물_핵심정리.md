# 산출물 실물 핵심 정리 — 1.13.1→1.13.2 첫 완주 (cycle-1.13.2-20260902)

서버 export를 사장님 Mac에서 무결성 확인(sha256 `67b128d3…` 일치) 후 직접 파싱한 핵심 값. 원본 묶음: `cycle-1.13.2-20260902.tar.gz` (72항목·208K).

---

## ① 계획 (om-plan) — `plan/`

**판정: approval (검토 준비 완료)** · `plan/validation-result.json`
- plan_digest: `sha256:b4813642fe2edb50…`
- input_lock_digest: `sha256:eb3a6331e77b7a87…` (사람 보관 봉인값)

**계획서 본문** · `plan/proposal.yaml` (mode: upgrade)
- 사람 결정(decisions): **24건**
- 영향 커스텀 ID: BANK-OM-001~007 (7개)
- 주요 섹션: 공식문서 발견(official-doc-findings), 문서-코드 교차확인, 업그레이드 계획, **경로 이동 유도(path-remap)**, 공유 영향, 매니페스트 증감(manifest-deltas), 실행할 계약(contracts-to-run)

> 그 외 계획 산출물 3건: 요청서(`run-request.yaml`), 검사기가 수집한 사실(`discovered-facts.json`), 독립 2차 문서판독(`independent-document-review.yaml`).

---

## ② 반영 (om-apply) — `apply/` + 제품 커밋

**판정: pass / static_consistent_awaiting_verify / eligible: true** · `apply/apply-result.json`
- 후보(candidate): `2a70bba2e9`
- 최종 트리 sha: `f925ab5dbaaa…`

**제품 코드 = 커밋 8개** (브랜치 `upgrade/1.13.2-apply-20260826`) · `apply/commits.txt`

| 단위 | 내용 | 파일 / 줄 |
|---|---|---|
| U1-backend | 은행 백엔드 재적용 | 13 / +1,476 |
| U2-spec | 스펙·스키마 재적용 | 20 / +5,461 −11 |
| U3-generated-ts | 생성 TypeScript 재적용 | 20 / +1,901 −8 |
| U4-ui | UI 커스텀 재적용 | 36 / +2,653 −25 |
| U5-i18n | 언어팩 키 병합 | 19 / +684 −38 |
| U6-service-icon | 은행 SVG 아이콘 보존 | 1 / +4 |
| U7-entityutils-split | EntityUtils 분해처 이식 | 3 / +58 −2 |
| U8-pureutils-relocation | Sybase/Tibero PureUtils 이관 | 2 / +5 |

> 그 외 반영 산출물 3건: 실행 기록(`execution-report.yaml`, 공유 경로 사람 승인 4건 포함), 요청서(`apply-request.yaml`), 결속값(`apply-context.yaml`).

---

## ③ 검증 (om-verify) — `verify/`

**판정: verified** · `verify/verify-receipt.json` (게이트 4개 전부 verified)

| 게이트 | 상태 |
|---|---|
| handoff_and_build_binding | verified |
| runtime_candidate_binding | verified |
| required_contract_tests | verified |
| ui_presentation_consistency | verified (not_configured — UI 부품 미제공) |

**계약 테스트 9종 — 전부 pass (fail 0)** · `verify/pytest/*.junit.xml` (직접 파싱 확인)

| 테스트 | 무엇을 확인 | 종류 |
|---|---|---|
| test_extended_column_projection | 은행 확장 컬럼이 API 응답에 실린다 | api |
| test_extended_column_rendered_row | 그 값이 화면 행에 실제로 그려진다 | 화면 |
| test_failed_assertion_projection | 실패한 데이터 단언이 API에 노출된다 | api |
| test_failed_assertion_rendered_row | 그 실패가 화면 목록에 나타난다 | 화면 |
| test_crud_search_roundtrip | InstanceCode 생성·조회·검색이 끝까지 동작 | api |
| test_hangul_composition_roundtrip | 한글 입력 조합이 편집기에서 안 깨진다 | 화면 |
| test_query_usage_roundtrip | QueryReport·조회 이력 연결 동작 | api |
| test_sybase_connection_schema_roundtrip | Sybase 연결이 스키마·화면·아이콘까지 일관 | 정적 |
| test_tibero_connection_schema_roundtrip | Tibero에 대해 같은 것 확인 | 정적 |

**빌드 증명** · `verify/build-receipt.json`: trust=local-issued, source_clean=true / **jar 실물검증 all_passed: true**(`build-verification.json` — 은행 클래스가 실제 이미지에 존재)

> 그 외 검증 산출물 3건 + 로그: 표본 데이터 증명(`fixture-receipt.json`), 검증 요청(`verify-request.json`), 환경값(`test-environment.REDACTED.json` — 토큰·세션 제거됨), pytest 로그 18건.

---

## ④ 확정된 등록 장부 — `관리파일/` (커밋 `0f6e2a04`)

이번 결과를 정식 등록으로 확정한 관리 파일 **18건**:
- 매니페스트 7건: BANK-OM-001 ~ 007 (ID별 담당 파일·계약·테스트)
- 장부 11건: 소유맵(`shared-path-owners`)·등록부(`customization-registry`)·계약(`contracts.yaml`)·커밋 인벤토리·변경 경로 목록 등

---

## 안전·무결성

- sha256 `67b128d3eb682de1…` 서버 값과 일치 확인.
- 비밀정보 제거됨: 인증 토큰·브라우저 세션 상태 제외(REDACTED), JWT·accessToken 재검색 잔존 0.
- 원본 불변: 사본 0444/0555 잠금, 저장소 변경 0.

> 각 파일의 한 줄 설명·봉인값 표·읽기 주의점은 묶음 안 `INDEX.md`(116줄)에 있음.
