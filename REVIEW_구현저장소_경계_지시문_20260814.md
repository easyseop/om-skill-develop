# `/om-plan` 구현 저장소 경계 검토 지시문

아래 검토는 `/om-plan` 코드를 어느 저장소에서 개발할지 확정하기 위한 마지막 읽기 전용 검토입니다. 결론을 미리 정하지 말고 실제 코드 의존성과 장기 재사용성을 함께 판단하세요.

## 검토 대상

1. `openmetadata-test`
   - 현재 검사기 코드: `harness/acgh/`, `harness/om_workflow.py`
   - 현재 Schema·Runtime Contract·등록자료·evidence·CI
2. `om-skill-develop`
   - 현재 `/om-plan` 설계 경위와 결정 기록
   - 현재는 코드가 없는 문서 저장소
3. 설계 패키지
   - `deliverables/claude-om-command-review-20260814/README.md`
   - `00_전체_슬래시커맨드_구성_로드맵.md`
   - `01_OM_SLASH_COMMAND_전체설계.md`
   - `01A_OM_PLAN_1단계_입출력_상세설계.md`
   - `03_구현_평가_반례계획.md`
   - `06_구현전_사람결정_질문목록.md`
   - `CLAUDE_OM_PLAN_2차_재검토_결과_20260814.md`
   - `proposed-control-files/` 전체

저장소가 서로 다른 경로에 있으면 먼저 실제 경로를 확인받으세요. 파일을 찾지 못한 상태에서 구조를 추측하지 마세요.

## 확인할 주장

다음 두 주장을 각각 실제 파일과 import 관계로 검증하세요.

### 주장 A — 기존 검사기 저장소에서 개발

- `/om-plan`은 기존 `verdict.py`, `candidate.py`, `gitprim.py`, Schema, 등록자료와 CI를 확장하므로 `openmetadata-test`에 구현하는 편이 자연스럽다.
- `om-skill-develop`는 결정 경위만 보관하고 구현 commit을 연결한다.

### 주장 B — 범용 저장소로 분리

- commit·tree 고정, 사실/제안 분리, digest, Hook, 재실행 규칙은 OpenMetadata에 한정되지 않는다.
- 범용 core를 별도 저장소에 두고 OpenMetadata 부분을 adapter로 두면 다른 제품에도 재사용할 수 있다.

## 반드시 비교할 네 선택지

| 선택지 | 내용 |
|---|---|
| A | 모든 구현을 `openmetadata-test`에 둔다. |
| B | 모든 구현을 `om-skill-develop`로 옮긴다. |
| C | 범용 core는 `om-skill-develop`, OpenMetadata adapter·등록자료·Contract·CI는 `openmetadata-test`에 둔다. |
| D | 1차 PoC는 `openmetadata-test`에 구현하되 `core`와 `integrations/openmetadata` 경계를 만들고, core API 안정 또는 두 번째 제품 연동 시 별도 저장소로 추출한다. |

## 판단 기준

1. 새 `plan-preflight`·`plan-validate`가 실제로 재사용해야 하는 기존 Python 모듈과 Schema는 무엇인가?
2. 그 의존성 중 범용으로 분리 가능한 것과 OpenMetadata 전용인 것은 무엇인가?
3. 저장소를 지금 분리하면 복사·중복·버전 고정·교차 저장소 CI가 얼마나 생기는가?
4. 기존 저장소에 두면 OpenMetadata 경로와 BANK-OM 개념이 범용 core에 스며드는 것을 어떻게 막을 수 있는가?
5. Hook·Skill 설치본, active registration, evidence, Runtime Contract, CI는 어느 저장소가 소유해야 하는가?
6. 한 저장소의 결과를 다른 저장소가 읽을 때 commit·Schema·checker version을 어떻게 고정할 것인가?
7. 두 번째 제품 adapter를 추가할 때 실제로 옮겨야 할 파일과 변경해야 할 import는 무엇인가?
8. 각 선택지의 1차 구현 비용, 운영 위험, 추후 분리 비용을 비교했을 때 어느 선택지가 가장 작고 되돌리기 쉬운가?

“CI는 코드와 같은 저장소에 있어야 한다” 또는 “범용이니 반드시 별도 저장소여야 한다”처럼 원칙만으로 결론내지 마세요. 패키지 의존성·재사용 모듈·CI 입력·관리자료 소유 위치를 실제 파일 단위로 제시하세요.

## 결과 파일

다음 파일 하나만 작성하세요.

```text
REVIEW_구현저장소_경계_결과_20260814.md
```

결과 순서:

1. 실제로 읽은 저장소·branch·HEAD·파일
2. 현재 코드 의존성 지도
3. 범용 core 후보와 OpenMetadata adapter 후보 파일 목록
4. A~D 비교표: 장점·단점·중복 위험·CI 구조·이전 비용
5. 권고 선택지 하나와 반대 선택지를 기각하는 구체적 근거
6. 선택한 구조의 최소 디렉터리 예시
7. 지금 구현 전에 반드시 고칠 설계 문구
8. 별도 저장소 추출을 다시 검토할 객관적 조건

## 금지 사항

- 어떤 저장소의 코드·설계·Git 설정도 수정하지 마세요.
- checkout·pull·reset·clean·commit·push·PR을 하지 마세요.
- 현재 dirty worktree를 정리하지 마세요.
- 테스트 개수나 일반론만으로 결론내지 마세요.
- 검토 결과를 작성한 뒤 구현을 시작하지 말고 중단하세요.

