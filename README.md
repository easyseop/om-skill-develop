# skill_develop — 슬래시 명령 설계 히스토리

OpenMetadata 운영 슬래시 명령(`/om-plan`, `/om-implement`, `/om-verify` …)을 만들면서
**세부 문제마다 무엇을 논의했고, 어떻게 해결했고, 무엇을 고려했는지**를 남기는 폴더.

설계 문서(코덱스 산출물)는 "결론"만 담긴다. 여기는 **결론에 이르기까지의 경위**를 담는다 —
나중에 "왜 이렇게 돼 있지?"가 나왔을 때 이 폴더부터 본다.

## 구조

```
skill_develop/
├── om_plan/        ← 1차 구현 대상. 문제별 기록
├── om_implement/   ← 후보 단계. 확정 전 논의만 쌓음
└── om_verify/      ← 후보 단계. 확정 전 논의만 쌓음
```

## 기록 형식 (문제 하나 = 파일 하나)

```markdown
# <문제 이름>

## 문제        ← 무엇이 왜 문제였나
## 논의 경과   ← 나왔던 대안들과 각각의 단점
## 결정        ← 무엇으로 정했고 왜
## 고려한 것   ← 결정하면서 짚은 주의점·한계
## 반영 위치   ← 어느 설계 문서·반례 ID에 들어갔나
## 미결        ← 아직 열려 있는 것
```

## 새 컴퓨터에서 시작

1. `CLAUDE.md`를 먼저 읽는다.
2. `/om-plan` 구현 상태와 다음 작업은 `om_plan/07_구현완료_및_다음작업_인수인계_20260814.md`를 따른다.
3. Claude 3차 검토에는 `om_plan/08_Claude_3차_구현검토_지시문_20260814.md`를 그대로 사용한다.

## 설계 당시 원본 자료

- 설계 패키지: `deliverables/claude-om-command-review-20260814/`
- 1차 적대적 검토: `…/CLAUDE_OM_PLAN_적대적_검토_결과_20260814.md`
- 2차 재검토(수정 확인): `…/CLAUDE_OM_PLAN_2차_재검토_결과_20260814.md`
- 사람 결정 대기 질문: `…/06_구현전_사람결정_질문목록.md`

위 설계 패키지는 당시 검토 원본이고 현재 구현 정본은 `easyseop/openmetadata-test`의
`codex/om-plan-impl-20260814`다. 다른 컴퓨터에서는 `om_plan/07`의 자료 목록으로
코드와 히스토리를 함께 복원한다.
