# `/om-plan` 구현 저장소 경계 검토 결과

> 검토일: 2026-08-14 · 읽기 전용 (수정·checkout·pull·commit·push 없음, dirty worktree 보존)

## 1. 실제로 읽은 저장소·branch·HEAD·파일

| 저장소 | 경로 | branch | HEAD | 상태 |
|---|---|---|---|---|
| openmetadata-test | `work/review-openmetadata-test` | `codex/om-1.13.1-rehearsal-baseline-20260806` (origin 대비 behind 2) | `b2dfb01a5ae1…` | dirty 16파일(문서류) — 보존, 기록만 |
| om-skill-develop | `skill_develop` | `main` | `347a03c` | clean. **코드 0, 문서 저장소** |
| 설계 패키지 | `deliverables/claude-om-command-review-20260814` | (비 git) | 매니페스트 102항목 검증 완료(2차 재검토) | 읽기만 |

읽은 파일: openmetadata-test의 `harness/acgh/` **49개 모듈 전수 목록 + import 관계 전수 추출(스크립트)**, BANK-OM/제품 경로 литeral 전수 grep, `harness/om_workflow.py`, `harness/pyproject.toml`, `.github/workflows/` 4종(그중 `source-candidate.yml` 정독), 루트 스크립트 14종 목록, `tests/bank/contracts/` 11개(1·2차에서 정독한 사본과 동일 계열). 설계 패키지 지정 문서 7종과 `proposed-control-files/`는 1·2차에서 전수 정독 — 이번에 경계 관련 절만 재확인.

## 2. 현재 코드 의존성 지도

`/om-plan`이 설계상 재사용하는 모듈에서 출발한 **import 폐쇄집합 = 15개 모듈** (acgh 49개 중):

```
verdict ── (의존 없음)                     gitprim ── (의존 없음)
binding → gitprim                          layout ── (의존 없음)
candidate → binding, gitprim, verdict      result_io → verdict
registry / manifest / contracts / shared_code / structdiff / policy_drift / upgrade_watch
initial_registration, registration_prep → (위 모듈들 조합)
```

- 폐쇄집합은 **자기완결** — 집합 밖 34개 모듈(patchkill, rehearsal, release 등)에 의존하지 않는다.
- `acgh`는 이미 **설치 가능한 Python package**다(`harness/pyproject.toml`, name=acgh v0.0.1). → 주장 B의 "교차 저장소 의존이 기술적으로 가능"은 사실. 단 배포 채널(사내 index/git tag pin)은 없음.
- CI(`source-candidate.yml`)는 이 저장소를 checkout해 `harness/tests` + `tests/bank/contracts`를 돌리고, `harness/registrations/kb-openmetadata/`의 실행 스크립트·등록자료를 **같은 저장소 경로로** 참조한다.

## 3. 범용 core 후보 vs OpenMetadata adapter 후보 (실측)

**BANK-OM literal이 코드에 박힌 모듈은 5개뿐**(initial_registration, registration_prep, shared_code(주석), survival, vendor_rebuild — 합계 ≈3,930줄). **`openmetadata-*`·`om-temp` 경로를 하드코딩한 acgh 모듈은 0개** — 제품 경로는 전부 데이터(registrations·policies)에 있다.

| 구분 | 파일 (실측 근거) |
|---|---|
| **범용 core 후보** | `verdict.py`(4-verdict·canonical_digest), `gitprim.py`, `binding.py`, `candidate.py`, `layout.py`(역할명 upstream/governance/extension — 제품 무관), `result_io.py`, `structdiff.py`, `policy_drift.py`, `upgrade_watch.py` + **신규로 만들 plan-preflight·plan-validate·마커 로직·재실행 규칙** |
| **OM adapter 후보** | `initial_registration.py`·`registration_prep.py`(BANK-OM 접두사·`CONTRACT-…-DRAFT` 명명 하드코딩: `:386,:404` 등), schema 12종(`"pattern": "^BANK-OM-[0-9]{3,}$"` 내장), `om_workflow.py`(`registrations/om-temp-<버전>` 규약), `registrations/` 데이터 전체, `tests/bank/contracts/`, CI 4종, 공식 문서 URL 규약(docs.open-metadata.org) |

즉 **"범용 메커니즘 / 제품 데이터·명명"의 경계는 이미 코드에 절반쯤 존재**한다. 문제는 등록 계열 모듈과 schema가 BANK-OM 패턴을 내장해 경계를 넘고 있다는 것 — 방치하면 새로 짤 plan 코드도 같은 방식으로 스며든다(주장 A의 실제 위험이자, 질문 4의 답이 필요한 이유).

## 4. A~D 비교표

| | A: 전부 openmetadata-test (경계 없음) | B: 전부 om-skill-develop | C: core→om-skill-develop, adapter 잔류 | D: openmetadata-test + core/integrations 경계 |
|---|---|---|---|---|
| 1차 구현 비용 | **최소** | 최대 — 15모듈+schema 복사 또는 git 의존 구축 | 큼 — 지금 당장 교차 저장소 CI·버전 고정 필요 | **최소+α** (디렉터리·경계 lint 1개) |
| 중복 위험 | 없음 | **높음** — acgh 두 벌 | 중간 — schema가 양쪽에 걸림 | 없음 |
| CI 구조 | 현행 유지 | 이중화: 코드 CI(신규)와 등록자료·Contract CI(잔류) 분리, plan-validate 최후 안전망이 등록자료와 딴 저장소 | 두 저장소 checkout + acgh 버전 pin을 input-lock에 추가 기록 | **현행 유지** (한 저장소) |
| 이전(추출) 비용 | **커짐** — BANK-OM이 새 코드에 스며든 뒤 분리(현 등록 모듈이 그 전례) | — | 이미 분리됨(단 core API 미확정 상태로 동결하는 셈) | **작음** — core 폴더째 이동 + import 경로 치환 |
| 역할 충돌 | — | **om-skill-develop의 "경위 저장소" 역할 파괴**(방금 정의한 규약 하루 만에 번복) | 위와 동일(부분) | 없음 |
| 되돌리기 | 쉬움 | 어려움 | 어려움 | **쉬움** |

주장 검증 결과: **주장 A는 사실**(15모듈 폐쇄집합+schema+등록자료+CI가 한 저장소에 응집). **주장 B도 원리상 사실이나 시기상조** — 분리 가능성은 pyproject 존재로 입증되지만, 분리 시점의 전제(안정된 core API)가 아직 없다: plan-preflight·plan-validate는 **아직 한 줄도 없다.** 존재하지 않는 API를 기준으로 저장소를 가르는 것은 경계를 두 번 긋는 일이다.

## 5. 권고 — **D** (기각 근거 포함)

**D 채택.** 1차 PoC는 openmetadata-test에 구현하되, 신규 plan 코드를 처음부터 두 구획으로 나눈다.

- **A 기각** — 경계 규율의 추가 비용이 사실상 0(디렉터리 + lint test 1개)인데, 경계 없이 두면 새 코드가 BANK-OM을 하드코딩하는 것이 **기본 경로**임을 기존 등록 모듈(`initial_registration.py:386` 접두사 검사, `:404` Contract 명명)이 이미 보여줬다. 이후 추출 비용이 그만큼 커진다.
- **B 기각** — 15모듈 폐쇄집합·schema 12종·등록자료·CI 4종을 복사하거나 git 의존으로 끌어와야 하고, plan-validate(최후 안전망)가 자신이 검증할 등록자료·evidence와 다른 저장소에 놓인다. 그리고 om-skill-develop은 오늘 "경위 저장소"로 정의·공표된 저장소다 — 코드를 넣는 순간 역할 재정의부터 다시 해야 한다.
- **C 기각** — 교차 저장소 비용(두 repo checkout CI, acgh 버전 pin, schema 소유권 분할)을 **지금** 지불하는데, 그 대가로 얻는 재사용은 **두 번째 제품이 없는 현재 0**이다. core API가 확정되기 전의 분리는 API 변경마다 두 저장소 동시 수정을 강제한다.

## 6. 선택한 구조의 최소 디렉터리 예시

```text
openmetadata-test/harness/acgh/
├── (기존 49개 모듈 — 당장 이동하지 않음. 폐쇄집합 15개는 core의 사실상 하부)
├── plancore/                      # 신규 — 제품 무관. BANK-OM·openmetadata·om-temp literal 금지
│   ├── preflight.py               #   입력 pin·사실 수집 오케스트레이션 (수집기는 주입받음)
│   ├── validate.py                #   재계산 대조·pointer 역참조·plan_digest·마커 정리
│   ├── markers.py                 #   session/run marker 수명
│   ├── rerun.py                   #   재실행 표 판정
│   └── schema/                    #   run-request·input-lock·facts·plan-result의 '형태'
│                                  #   (ID 패턴 등 제품 규칙은 config로 주입, 스키마에 미내장)
└── integrations/om/               # 신규 — OpenMetadata/BANK-OM 어댑터
    ├── config.py                  #   id_pattern="^BANK-OM-…", registrations 경로 규약, 문서 URL 시작점
    ├── collectors.py              #   registry·manifest·shared_code 등 기존 모듈 호출부
    └── doc_sources.py             #   공식 문서 수집(raw bytes·버전 token)

harness/tests/test_plancore_boundary.py   # 경계 lint: plancore/* 에 BANK-OM|openmetadata|om-temp literal 0건 단언
```

`om_workflow.py`에 `plan-preflight`·`plan-validate` 서브커맨드 추가(라우터는 integrations/om을 로드). Hook·Skill 설치본, active registration, evidence, Runtime Contract, CI는 전부 **openmetadata-test 소유**(질문 5). om-skill-develop은 각 결정 파일의 "반영 위치"에 **구현 커밋·브랜치 링크만** 추가한다(질문 6 — 교차 참조는 commit SHA 링크와 input-lock의 checker catalog digest로 충분, 코드 의존 없음).

## 7. 지금 구현 전에 반드시 고칠 설계 문구

1. `01_OM_SLASH_COMMAND_전체설계.md` §11 구현 순서 — 구현 위치와 경계를 명시: "plan 신규 코드는 `acgh/plancore`(제품 무관)와 `acgh/integrations/om`으로 분리한다. plancore에는 BANK-OM·openmetadata 경로·om-temp 규약을 넣지 않고, ID 패턴·경로 규약·문서 출처는 integration 설정으로 주입한다."
2. 같은 문서 §10 재사용 자산 — "재사용은 import로 하며 복사하지 않는다. 기존 등록 모듈의 BANK-OM 하드코딩 방식을 plancore에 반복하지 않는다" 한 줄 추가.
3. `03_구현_평가_반례계획.md` P0 목록 — 경계 lint test(`plancore` 금지 literal 0건)를 반례로 추가.
4. `06_구현전_사람결정_질문목록.md` — 저장소 선택이 질문으로 없다면 답 확정 기록으로 D를 남긴다(사람 승인 후).

## 8. 별도 저장소 추출을 다시 검토할 객관적 조건 (전부 충족 시)

1. **두 번째 제품 어댑터의 실요구**가 발생함 (가정이 아니라 실제 대상 제품)
2. plancore의 공개 API·schema가 **실전 버전업 1회(예: 1.13.x→1.13.y) 예행연습을 무변경으로 통과**함
3. 경계 lint test가 도입 후 **위반 0건**으로 유지됨 (경계가 실제로 지켜졌다는 증거)
4. acgh(또는 plancore) 패키지의 **버전 고정·배포 채널**(사내 index 또는 git tag pin)이 준비되고, input-lock의 checker catalog digest가 그 버전을 식별함

조건 미충족 상태의 조기 추출은 C의 비용을 선불하는 것과 같으므로 하지 않는다.

---
*읽기 전용 검토. 이 결과 파일 1개만 작성했고 commit·push·구현은 하지 않았다.*
