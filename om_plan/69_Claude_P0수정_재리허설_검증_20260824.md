# Claude 독립 검증 — om-apply P0 수정 + 재리허설 (결과서 68)

검증일: 2026-08-24. 판정: **통과** — om-apply v1이 실데이터에서 목표 상태 도달.

## 직접 확인한 것

1. **P0-1 수정 실측**(apply.py): `all_owned_paths`(전 ID union)는 **등록 밖 검출에만**(L151-152 유지), "최종 효과 없음" 검사는 **`planned_owned_paths`에만**(L130·154). L172의 전체 union 루프는 공유 경로 도출용(정당 — 여러 ID 소유 감지는 전체를 봐야 함). **역할 분리 정확, 게이트 약화 없음.**
2. **P0-3 실측**: 활성 `shared-path-owners.yaml`에 2경로 추가, 소유자 `[BANK-OM-006, BANK-OM-007]`(매니페스트 실측 일치), **총 39 = 매니페스트 파생 39, 차이 0.**
3. **새 반례 실재**: `test_single_id_change_ignores_unplanned_id_manifest_paths`(정상 단일 ID → awaiting_verify), `test_all_ids_named_by_planned_units_have_final_effect_checked` 등 4개.
4. **전체 239개(235+4) 테스트 — 내 실행으로 전부 통과**(exit 0).
5. **원본 검사기**: HEAD `7c544efb2b` 불변, 이력 깨끗(스냅샷 커밋 잔재 없음), 미커밋 구현 12파일 보존.
6. **결과서(68) 값 정합**: 정상 change `pass/static_consistent_awaiting_verify` + `eligible:true`(BANK-OM-005 계약테스트 1개), scope probe `block`+`eligible:false`, digest 전부 기록.

## 의미

- **om-apply v1이 실제 1.13.1 데이터에서 "정상 변경은 통과, 우회는 차단"을 동시에 달성.** 리허설이 찾은 P0 결함이 고쳐졌고, 고치면서 기존 차단 게이트가 약해지지 않았음을 반례로 확인.
- 계약 테스트는 여전히 **미실행**(claim only) — 그건 om-verify(3단계) 몫. awaiting_verify payload(candidate SHA/tree + required tests)가 그 인계 지점.

## 남은 것 (다음 결정·작업)
1. om-apply 변경(12파일 미커밋)의 **커밋 분리·push 여부 — 사람 결정.**
2. **om-verify(3단계) 설계·구현** — awaiting_verify를 받아 실제 계약 테스트를 실행할 소비자.
3. 기록된 후속: P0-2(활성 잠금, R-1·R-2와 함께)·P1-2~P1-5·upgrade 독립 문서검토 배선.
