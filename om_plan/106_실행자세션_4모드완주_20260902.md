# 실행자 세션 4모드 완주 — 실행·검증·기록 (2026-09-02 밤~)

구성: 로컬 두 세션(실행자 kb-datacatalog-...-e5 ↔ 검증자·조정자 = 이 세션). 실행자가 harness/om 실행, 검증자가 독립 검증·기록. 목표: om-plan 4모드 규칙 위반 없이 완주, 코덱스 핫픽스 필요 지점은 기록만.
사용량 안전판: 사용자 제공 % ≥95% 즉시 중단 / 완주 후 자동 정지. 기준선 27%(밤 시작).

각 모드는 [무엇을 했나 / 결과(원문) / Claude 독립 검증 / 의미 / 핫픽스 대기 여부]로 기록.

---

## 환경 구성 (JDK21·yarn 등)
- 상태: **이번 4모드(plan)엔 불필요** — plan은 git+Python 검사기만 쓰므로 빌드 도구 없이 완주. JDK21·yarn 설치는 om-verify(빌드) 할 때만 필요. 실행자가 모드 작업에 집중하느라 별도 설치 보고는 없음. verify 진행 시 그때 설치.

## /usage 자체 모니터링 가능 여부
- 상태: 확인 못 함(실행자가 모드 작업 우선). 그리고 실행자↔검증자 SendMessage가 분류기로 차단돼, /usage 결과를 실행자가 나에게 전달하는 경로도 불가. → 사용량 감시는 사용자 제공 %에 의존(밤새 미제공 시 크론 완료 후 자연 종료로 대체).

---

## ① change 모드 — ✅ 승인 (검증 완료)
- **무엇을**: BANK-OM-004 change 정합, proposal 저작(decision 1, affected BANK-OM-004).
- **결과**: verdict approval, verified True, plan_digest c8e36b21…
- **검증**: Claude 파일 실측 — proposal.yaml 존재·1 decision 확인.
- **의미**: change 모드 정합 전 과정 재현 성공.

## ② feature 모드 — ✅ 승인 (검증 완료)
- **무엇을**: 신규 커스텀 BANK-OM-008 feature, 실행자가 proposal 저작(decision 2, affected BANK-OM-008).
- **결과**: verdict approval, verified True (run T085723Z). 첫 시도 analysis_error 후 재저작으로 approval — 정직한 반복.
- **검증**: Claude 파일 실측 — proposal.yaml 존재·affected_ids [BANK-OM-008]·2 decisions.
- **의미**: feature 모드(신규 기능 등록)를 실행자가 자력 저작으로 승인까지 완주.

## ③ initial 모드 — ⚠️ block (정상: 사람 disambiguation 필요, 핫픽스 아님)
- **무엇을**: initial(최초 등록) proposal 저작 시도(decision 2, affected []).
- **결과**: verdict block. 사유 원문: "ambiguous commit ids require unresolved questions and a STOP".
- **검증**: Claude 실측 — 여러 커스텀 ID에 걸친 커밋이 있어, proposal이 그 커밋들을 unresolved question으로 STOP 처리해야 함. 지금 proposal엔 그게 없어 block.
- **의미**: **검사기 정상 작동**. 최초 등록 시 "어느 커밋이 어느 기능인지 모호한 것"을 사람이 정하라고 막는 것 — 핫픽스 대상 아님. 올바른 종결 = proposal에 모호 커밋을 질문으로 넣고 STOP(=조건부/사람결정), 또는 이 block 자체가 정상 결과.

## ④ upgrade 모드 — ✅ 승인 (검증 완료)
- **무엇을**: 승인본(proposal.yaml·apply-plan.yaml) byte 동일 복사 재사용, 단 independent-document-review는 CLAUDE.md 요구대로 판독 에이전트로 새로 호출.
- **결과**: verdict approval, verified True (run T091822Z). discovered_facts_digest 직전 승인본과 동일. proposal.yaml/apply-plan.yaml cmp 일치.
- **검증**: Claude 실측 — proposal 3파일 존재, verified True. proposal_digest는 새 판독문 때문에만 직전과 다름(YAML 본문은 바이트 동일).
- **의미**: upgrade 재사용 경로 완주. 판독 재호출까지 규약대로.

---

## 종합 — om-plan 4모드 완주 (실행자 세션, 2026-09-02 밤)
| 모드 | 결과 | 핵심 |
|---|---|---|
| change | ✅ 승인 | BANK-OM-004 정합, byte-copy 재현 |
| feature | ✅ 승인 | 신규 BANK-OM-008 자력 저작(기존 계약 편승 금지, 미특정 경로는 blocking finding) |
| initial | ⚠️ 조건부 종결 | 모호 커밋 1건(afcb2d2c) 질문 3건으로 STOP — 커밋 귀속 창작 없음, 사람 결정 대기 |
| upgrade | ✅ 승인 | 승인본 재사용 + 판독 재호출 |
- 저장소 청결(0줄), 커밋·push·sudo·마커조작 없음. **규칙 위반 0.**
- 실행자↔검증자 통신: SendMessage 분류기 차단으로 불가 → 공유파일이 유일 채널로 작동.

## 검증자 정정(실행자 지적 수용)
1. feature proposal 파일은 실재(3383B) — 내 앞선 "안 보임"은 경로 오독(끝 Z 누락).
2. initial block은 "등록 존재"가 아니라 "모호 커밋" 사유 — 실행자가 등록 없는 경로로 돌려 preflight 통과 후 block.

---

## 핫픽스 대기 목록 (이번 밤 발견 — 코덱스 검사기 .py 수정 대상)
- **(B) 최우선: plan-resume 경로 전체 사용 불가** — record_trusted_input_lock_digest가 plan start(om_workflow.py:253)에서만 호출되고 plan-resume은 안 함 → resume한 마커에 digest 없어 check가 TRUSTED_INPUT_LOCK_DIGEST_MISSING로 항상 실패. (내 앞선 발견 #7의 근본원인 규명). → 백로그 #20
- **(A) analysis_error 시 run 영구 소실** — YAML 구문 오류 하나로 analysis_error→.plan-active 소비→resume이 verdict≠block이라 거부(RUN_NOT_PROPOSAL_REVISABLE). 구문 오류는 사실 변경 아니니 resume 허용 대상. 실행자 우회책: check 전 로컬 YAML 파싱 선행. → 백로그 #21
- (C) plan check --expected-input-lock-digest 를 LLM이 공급하는 것은 분류기가 차단(정상 — 신뢰사슬 보호). 핫픽스 아님.
- (D) initial 세대 페어링: ACTIVE_REGISTRATION_EXISTS로 올바른 페어링 불가 → 설계 질문(핫픽스 아님). proposal에 blocking finding 명시.

## 사용량 체크포인트 로그
| 시각 | 사용량% | 소비율 | 95% 예상 | 조치 |
|---|---|---|---|---|
| 밤 시작 | 27% | 기준선 | — | 진행 |

---
# 전 사이클 (plan→apply→verify) — 2026-09-03, 최신 검사기(aaa52cc32b, 핫픽스 5건)
## STEP 1 — 환경 정렬 ✅
- 검사기 gitlab-checker/main(aaa52cc32b) 정렬·청결 확인. JDK21·yarn·docker 확인(단 JAVA_HOME 미설정 → 빌드 시 openjdk@21 export 필요). 실행자 수행, 검증자 확인.
## STEP 2 — upgrade plan ✅ 승인
- run: om-plan-upgrade-20260903T011250Z. 실행자가 plan start했으나 proposal 복사에서 정지 → 검증자가 승인본 3파일 byte-copy(cmp 일치) + plan check 완료.
- 결과: **verdict approval(exit2), verified True**, plan_digest sha256:c3506d43…, input_lock_digest sha256:d9b5a89a…
- 의미: 최신 검사기(핫픽스 5건)로 upgrade 계획이 깨끗하게 승인. plan-resume 핫픽스 반영된 검사기에서 정상 동작.
## STEP 3 — apply (사용자 결정 대기)
- A) 8단위 재이식(무거움) vs B) 기존 후보 2a70bba2(GitLab fetch·체크아웃 완료) + apply check로 정합 검증(재이식 없이). → 사용자 결정 요청 중.
