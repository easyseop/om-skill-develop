# Mac 재현 중 발견 — 추가 개선·핫픽스 필요 목록 (2026-09-02~)

이 파일에 재현 중 나온 모든 "고쳐야 할 것"을 누적 기록. 나중에 코덱스 개발 요청서로 일괄.

| # | 무엇 | 어디서 발견 | 유형 | 상태 |
|---|---|---|---|---|
| 1 | 훅 마커 게이트가 Mac raw CLI 실행에선 검증되지 않음 — 실배포는 훅이 강제하나, 로컬 재현은 CLI 직접 실행이라 우회됨. Mac 재현의 구조적 한계(안전 검증엔 실배포 훅 환경 필요) | Mac 환경 준비 | 재현 한계 | 기록됨 |
| 2 | digest는 환경 결속(input_lock에 checker HEAD·tree·경로 포함) → 다른 기계선 같은 verdict라도 digest 다름. Mac plan_digest a8230d26 ≠ 서버값. 재현성 문서에 "cross-machine은 verdict 동일·digest 상이" 명시 필요 | om-plan Mac 재현 | 문서/설계 | 기록 |
| 3 | run 디렉터리가 예상 밖 위치(.git/worktrees/.../om-plan-evidence)에 생성 — 검사기가 review-openmetadata-test의 linked worktree라 .git이 부모 admin dir로 해석됨. 독립 저장소로 클론했으면 안 겪음. 재현 가이드에 "검사기는 linked worktree 아닌 독립 clone 권장" 필요 | om-plan Mac 재현 | 환경/문서 | 기록 |
| 4 | raw CLI 실행자가 exit 2=approval을 "예상 밖"으로 오해 — exit 코드 의미(0 pass·1 block·2 approval·3 error)가 CLI 출력 자체엔 안 나옴. 실행자용 안내나 CLI stdout에 exit 의미 힌트 추가 고려 | om-plan Mac 재현 | UX | 기록 |
| 5 | **apply 결과 미보존 리스크** — 서버 apply 8커밋(candidate 2a70bba2)이 원격에 push된 적 없어 서버 로컬에만 존재. Mac·GitLab 어디에도 없음. verified까지 끝난 후보가 단일 기계에만 있는 건 운영 리스크(서버 유실 시 재이식 필요). 처방: verified 후보는 사람 승인으로 원격 push(보존용 브랜치)하거나 export에 diff/번들 포함 | apply Mac 재현 | 리스크/운영 | 기록 |
| 6 | 요청 스키마 학습(문서화 필요): refs는 **모든 모드 필수**(upgrade만 아님) / change_path는 파일경로 아닌 **enum**(pre_plan·post_change_reconcile) / initial refs는 current_custom+official / upgrade refs는 official_base·official_target·custom_baseline. 모드별 요청서 예시가 저장소에 없어 시행착오 발생 → references/request-format.md에 모드별 필수필드·예시 추가 필요 | om-plan 모드 탐색 | 문서 | 기록 |
| 7 | change 재검사 루프(check→block→수정→recheck)가 raw CLI에선 막힘 — 재검사 시 세션 마커에 trusted_input_lock_digest 필요한데 이건 훅이 넣는 값. plan-session-start(CLI)는 안 넣어 TRUSTED_INPUT_LOCK_DIGEST_MISSING. **우회책: 처음부터 완전한 proposal 저작하면 단일 check로 승인**(resume 불요). 문서화 필요 or CLI resume에 digest 인자 추가 | change reconcile | UX/설계 | 기록 |
| 8 | change proposal 필수 필드 학습: 각 decision에 evidence_refs·required_follow_up 필수(없으면 block), 대상 ID의 등록 계약 테스트 전부 required_tests에 포함 필수. 모드별 proposal 예시 부재로 시행착오 → references에 change proposal 최소예시 추가 필요 | change reconcile | 문서 | 기록 |
