# 1.13.1 → 1.13.2 업그레이드 가이드 (스킬 파이프라인 실전 수행)

작성일: 2026-08-25. 수행: **검사기 저장소에서 연 새 Claude Code 세션**(사람 동승). 이 문서 하나로 시작~끝 진행 가능.
**성격**: 파이프라인의 본래 목적(커스텀 보존 버전업)의 첫 실전. 스킬 실기동이 0회 상태라면 **이 세션이 스모크(지시서 92)를 겸한다** — 92의 관찰 기록 의무(S0·S2: 스킬 인식, marker 생성, 훅 거부, exit 2 보고 정확성)를 결과서에 포함하라.
**대원칙**: 문제가 나오면 그게 성과. 게이트 우회·수기 보정·재해석 금지. 각 단계 사이 사람 승인 지점에서 멈춘다.

## 0. 준비물 (스킬 호출 전 — 훅이 걸리면 Write가 거부되므로 먼저)

```bash
cd /Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/work/kb-datacatalog-upgrade-checker-om-plan-cli
git pull    # 스킬 3종·harness/om 포함 main 확인
```

- **제품 저장소**(업그레이드 전용, 2026-08-25 Claude가 사전 구성·검증 완료): `/Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/work/om-upgrade-20260825/product`
  - HEAD = `8ac18ad053`(커스텀 완성, GitLab `datacatalog/kb-datacatalog` main과 동일) — 실측 재확인만.
  - 공식 1.13.2 태그 이미 수신: `1.13.2-release` = `2763bf97ce` — `git rev-parse 1.13.2-release`로 재확인만.
- **run-request(upgrade) 작성**: `harness/acgh/plancore/schema/plan-run-request.schema.json`의 upgrade 절을 실측해 필수 필드를 채운다(mode: upgrade, refs.official=1.13.2 SHA·refs.current_custom=8ac18ad053, 공식 문서 입력 등). om-plan SKILL.md의 upgrade 행(3층 검증·필수 산출물 9종·path-remap)이 안내 기준.
- 참고(선택): 검증 단계 선행물 절차 자산 — 지난 리허설의 build receipt 발행 스크립트: `work/om-verify-rehearsal-20260824-01/build_candidate_and_issue_receipt.sh`, `Dockerfile.local-base`(Alpine CDN 장애 시 우회 — 쓰면 정직 기록).

## 1. 계획 — `/om-plan`

- `/om-plan 1.13.1→1.13.2 업그레이드 계획 run을 시작해줘. 요청서는 <경로>` 입력.
- 스킬 절차대로: `harness/om plan start` → 제안서 작성(upgrade 필수 산출물 — path-remap 커버리지·doc-code-crosscheck 포함) → `harness/om plan check`.
- **성공 = exit 2(approval·review_ready)** — 0이 아님을 재확인. block(1)이면 reasons를 고쳐 재검증(7단계 재개 규율), analysis_error(3)면 원인 보고 후 중단.
- 🛑 **사람 승인 지점 1**: 계획서(특히 path-remap·영향 커스텀 ID·3층 검증 결과)를 사람이 검토·승인. 승인 없이는 다음 단계 금지.

## 2. 반영 — `/om-apply`

- 승인된 plan run과 `plan_digest`로 apply request 작성 → `/om-apply` 호출.
- `harness/om apply start` → 승인 DAG 순서로 upstream 병합·커스텀 보존 구현(단위별 커밋) → `harness/om apply check --final-ref <SHA>`.
- **병합 충돌·실패 시 3단 복구만**: ①계획된 동작 복원 ②새 계획 요청 ③사람 — 도달 단계를 기록. **STOP·scope_variances 규율 준수**(민감 경로 즉시 중단, 그 외 사유 4요소+사람 승인).
- 성공 = `static_consistent_awaiting_verify` + `verify_handoff` — 값 그대로 인계.
- 🛑 **사람 승인 지점 2**: scope_variances가 하나라도 있으면 사람 승인 후에만 재개.

## 3. 검증 — `/om-verify`

- 선행물: build receipt(candidate 이미지 빌드 — 수십 분 소요 정상) + fixture receipt + **run 전용 fresh compose**(상주 lab 8585 불변! 별도 포트·project·볼륨).
- verify request 작성 → `/om-verify` 호출 → `harness/om verify run`.
- 판정 3상태 그대로 보고(skip·부분·WARN ≠ 통과). receipt의 trust_limitation 문구 포함. infra_error면 원인 수정 후 **새 run-dir**로.
- 종료 후 fresh 환경 정리(compose down·볼륨 제거)를 기록.

## 4. 보고와 사람 결정

- 결과서 `skill_develop/om_plan/95_업그레이드_시도_결과_20260825.md`: [92 관찰 기록 / 각 단계 실값(SHA·digest·exit·receipt) / 발견 문제 전부 / 남은 위험]. verified여도 **배포 아님** — 통합 승인은 사람.

## 금지·주의

- 커밋은 apply의 계획된 단위 커밋(제품 저장소, 로컬)만. **push·MR·배포 금지**(사람 결정 후 별도).
- 상주 lab(8585)·활성 등록자료 원본 불변. 검사기 저장소 코드 수정 금지(스킬은 실행자이지 수정자가 아님).
- 알려진 미결로 막히면 우회하지 말고 보고: Q10 재베이스 정책(임시), upgrade 문서검토 2차 독해는 LLM 교차확인일 뿐(결정 권한 없음), 훅 관련 이상(92 참조).
