# Codex 지시 — 검증완료 스택 커밋 분리 (push 없음)

작성일: 2026-08-21. 작성자: Claude(검증자·기록자). 구현: Codex.
전제: 아래 3덩이는 Claude 반례검증 통과(결과서 47·49·52·검증 50·53). 현재 브랜치 `codex/om-plan-verified-gates-20260820`(HEAD `df970075f4`) 위에 **미커밋으로 뒤섞여** 있다.

> ★ **GitLab 반영 보류 중**(사유: 파이프라인·4모드 미검증 + Q9 미반영). **이번 지시 = 로컬 커밋 분리까지만. push·MR 절대 금지.**

## 원칙
- **검증 통과분만.** 미검증·실험 변경 섞지 마라.
- **논리 단위로 분리.** 한 파일에 여러 기능이 섞였으니 `git add -p`(hunk 단위)로 나눠 스테이징.
- **각 커밋 후 전체 테스트 green 유지**(의존 순서대로 쌓기).
- 어느 hunk가 어느 기능인지 **불확실하면 커밋하지 말고 결과서에 질문**으로 남겨라.

## ★ 정정: A-4는 이미 커밋됨 (`55de4014ea`)
- git log 확인: `55de4014ea A-4: preserve per-ID relationship facts` + Q11 3층(`23e65b2448`)·hooks(`9a10085fc0`)·CI trim(`2194c414ab`)·`df970075f4`가 **이미 로컬 커밋**돼 있다(단 gitlab-checker/main엔 미push).
- **따라서 지금 미커밋 스택 = ① direct_tests 제거(옵션3, A-4 후속) ② A′ ③ A-2 세 개.** A-4 본체는 커밋1로 만들지 마라(이미 있음).

## 미커밋 스택 (git diff --stat 기준)
- `harness/acgh/integrations/om/collectors.py` — **두 기능 섞임**: `_registration_metadata`(direct_tests 제거) + `validate_proposal`(A′)
- `harness/acgh/plancore/validate.py` — A-2
- `harness/tests/test_om_registration_facts.py` — direct_tests 제거(A-4 테스트 개정)
- `harness/tests/test_plan_workflow.py` — **두 기능 섞임**: A′ 테스트 + A-2 테스트
- `harness/tests/test_om_plan_ci.py` — A-2 fixture 보정

## 커밋 분리 (이 순서 = 의존 순서, 각 커밋 후 green)

### 커밋 1 — direct_tests 제거 (A-4 후속, 계약 테스트만)
- `collectors.py`의 **`_registration_metadata` 안 hunk만**: 미커밋 hunk `@@ -109`·`@@ -150`·`@@ -161` (parts에서 direct_tests 제거 + 관계 tests=계약만).
- `test_om_registration_facts.py` 전체(direct_tests 미포함 단언으로 개정된 것).
- 메시지: `A-4 follow-up: drop manifest direct_tests from relationship facts (contract tests only)`
- 근거: 명세 40(개정판·계약only), 지시 48, 검증 50. 사람결정 D-directtest 옵션3.

### 커밋 2 — A′ change 테스트 누락 방지 게이트
- `collectors.py`의 **`validate_proposal` 안 hunk만**: 미커밋 hunk `@@ -602`(change 블록 관계 fact 소비·누락 block).
- `test_plan_workflow.py`의 **A′ 관련 테스트 hunk만**(함수명에 `aprime`).
- 메시지: `A-prime: block change proposals missing a customization's registered tests`
- 근거: 명세/지시 46, 결과 47, 검증 50. A-4(커밋1) fact에 의존하므로 순서 뒤 X.

### 커밋 3 — A-2 decision 필드 타입 검사
- `validate.py` 전체(`_validate_required_decision_fields` 타입검사).
- `test_plan_workflow.py`의 **A-2 관련 테스트 hunk만**(함수명에 `a2_`).
- `test_om_plan_ci.py`(fixture `required_follow_up` 리스트화).
- 메시지: `A-2: type-check decision fields in proposal validation`
- 근거: 지시 51, 결과 52, 검증 53.

## 검증 (각 커밋 후, 최종 후)
- 각 커밋 직후 `PYTHONPATH=.:harness python3.11 -m pytest -q harness/tests` green.
- 최종 3커밋 후에도 green(=210 통과).
- `git log --oneline -3`로 3커밋 분리 확인.

## 금지
- **push·MR·원격 조작 금지.** 로컬 커밋만.
- 검증 안 된 변경 추가·기존 커밋 수정 금지. 제품코드·활성 등록자료·SKILL/references 수정 금지.
- hunk 귀속 불확실 시 커밋 말고 질문.

## 결과
`skill_develop/om_plan/57_Codex_커밋분리_결과_20260821.md`: 3커밋 SHA·각 포함 파일/hunk·각 커밋 후 테스트 결과·불확실해 남긴 것. **push 안 함** 명시.
