# Claude 독립 검증 — A-2(decision 타입검사) + 트랙 A 완료 선언

검증일: 2026-08-21. 대상: `work/kb-datacatalog-upgrade-checker-om-plan-cli` 브랜치 `codex/om-plan-verified-gates-20260820`(미커밋). 근거 결과서: `52`(A-2). 지시서: `51`.

## 판정: 통과 (P0/P1 없음)

### 1. 코드 실측 (validate.py:216-263)
- `_validate_required_decision_fields`에 present-조건 타입검사만 추가 확인: subject/decision=비어있지 않은 str, evidence_refs=list, affected_customization_ids=문자열 list, required_follow_up=list|"none". decision_source enum·기존 로직 불변.

### 2. 내가 직접 실행
- `pytest harness/tests` = **210 passed**(내 실행). a2 반례 26개 포함.

### 3. 독립 반례 (타입 함수 직접 호출)
- affected=문자열 / affected 원소 정수 / subject 공백 / evidence_refs=문자열 / required_follow_up=매핑 → 각각 정확한 issue 생성. 정상·required_follow_up=리스트 → issue 0. **subject 부재 → "missing subject" 한 건만**(이중보고 없음). 전부 기대대로.

### 4. 정상 제안서 거짓 block 없음
- 4모드 example 제안서 타입 issue 0 (Codex 보고 재확인 + 내 반례에서 정상 케이스 [] 확인).

### 5. 수용기준 #3 (evidence_refs) — 무해한 라벨 차이, 조치 불요
- 지시서는 "evidence_refs 타입오류 → block"을 요구했으나, 실제로는 기존 `_validate_refs`가 먼저 실행돼 **analysis_error(exit3)**로 반려된다.
- **내 실측**: `_validate_refs`는 비-리스트 evidence_refs(문자열/매핑/정수/None) **전부** `PlanControlError("evidence_refs must be a list")`로 **예외 발생** → analysis_error. 어떤 비-리스트도 통과 못 함.
- **판정**: block(1)과 analysis_error(3)는 **둘 다 fail-closed(반려)**. 내 지시서의 "block" 요구가 과도했던 것이고, 구조적으로 깨진 evidence_refs를 analysis_error로 반려하는 현재 동작이 오히려 타당. **코드 변경 불요.**
- (재검토 여지: 판정 라벨 통일을 원하면 호출 순서 조정 — 그러나 안전상 차이 없어 지금 불필요. 굳이 사람 결정 대상 아님.)

## 트랙 A(doc 38) 완료 상태

| 항목 | 상태 |
|---|---|
| 1. proposal 형식 검사(=A-2) | ✅ 형식 핵심 기존 강제 + 타입검사 보강 완료·검증 |
| 2. change 관측 분기(A-3) | ✅ A′(테스트 누락 방지)로 재규정·구현·검증. 기준선 잠금(A)은 apply 개방 후(R-1·R-2) |
| 3. facts ID별 관계 보존(A-4) | ✅ 구현·검증 (+ direct_tests 제거 D-directtest 옵션3 반영·검증) |
| 4. A5 되돌리기 | ✅ P1-1 결정대로 되돌림·검증 |

**→ 트랙 A(계획 품질 강화) 완료.** 남은 것은 트랙 B(하부구조·사람결정)와 **미반영분의 GitLab 검증 관문**.

## 현재 미커밋 스택 (같은 브랜치)
1. A-4 per-ID facts + direct_tests 제거
2. A′ change 테스트 누락 방지
3. A-2 decision 타입검사
- 전부 Claude 반례검증 통과. **push·MR 안 함**(GitLab 보류: 파이프라인·4모드 미검증 + Q9 미반영).

## 다음 관문 (사람 필요)
① 미커밋 스택을 논리단위 커밋으로 분리(로컬, doc 43) → ② Q9 GitLab 반영 → ③ MR 브랜치 파이프라인 + 4모드 end-to-end 초록불 → ④ 사람 Merge 승인.
