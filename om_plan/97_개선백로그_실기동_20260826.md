# 개선 백로그 — 실기동에서 나온 항목 (우선순위·상태 관리 정본)

| # | 항목 | 유형 | 우선순위 | 상태 |
|---|---|---|---|---|
| 1 | 훅 이벤트 순서(Expansion 발급소화) | 결함 | P0 | ✅ 해소 (핫픽스 04e9982663, MR !4) |
| 2 | Stop훅이 intent_review 사람 대기를 밀어냄 | 인체공학 | P1 | 열림 (I2) |
| 3 | analysis_error(INPUT_UNREADABLE) 재개 불가 — 오타 1자에 run 전체 재시작 | 인체공학 | P1 | 열림 (I2) |
| 4 | 교집합(공식변경∩등록경로) fact 부재 — 계획 작성자가 수작업, 30분·21만+토큰 | 비용 | P1 | 열림 (I3) — 검사기에 fact 추가 제안 |
| 5 | 보호 세션 중 읽기 전용 보조(grep류) 전면 차단 — 4번의 원인 절반 | 인체공학 | P2 | 열림 (읽기 전용 명령 허용 목록 검토) |
| 6 | 내려받은 셋업 스크립트 직접 실행 차단(auto mode) — 저장소 정식 스크립트로 편입하면 해소 | 인체공학 | P2 | 열림 |
| 7 | om-resume 스킬 파일 신설 여부 | 결정 | P2 | 열림 (24) |
| 8 | 요청서 대화형 도우미(슬래시 하나로 문답) | UX | P2 | 열림 (skill-invocation-ux) |
| 9 | **사람 결정 요청 표준형식 부재** — 시스템·세션이 항목 나열만 하고 선택지·결과·권고를 사람 눈높이로 제시 안 함(권고 주체 명시도 없음). 스킬 보고 형식에 [쉬운 설명/선택지/각 선택의 결과/권고+주체+근거] + **최상단 3줄 요약(무슨 일/상태/사람 할 일)** 강제 필요 | UX | **P1** | 열림 (사용자 지적 2026-08-26) |
| 10 | **plan→apply 인터페이스 게이트 미배선** — ① apply-plan.yaml 자체를 plan 게이트가 요구 안 함(I4) ② start_commit_sha 역할 검증 부재(I6) ③-보강: 계획 내부 정합 검사도 부재 — manifest-deltas·management_expectations ↔ management_files 허용 목록 상호 대조를 기계 검증이 안 함(자기모순 계획이 approval 통과, apply 장부 단계에서야 딜레마 정지). plan check에 필드 간 정합 추가 ③ repository_id 신원 계산 불일치 — plan은 요청서 별칭을 존중(preflight.py:43-58), apply는 remote URL만 읽음(workflow.py:308-310) → 선택 필드를 채우면 반드시 APPLY_REPOSITORY_ID_MISMATCH(I7). 실행계획서 요구+역할 검증+신원 계산 통일을 plan check로 전진 배치 | 결함 | **P1** | 열림 (I4·I6) |
| 11 | **2차 판독 비수렴** — 같은 문서 4회 판독에서 매번 신규 발견(16→10→0→2), block 루프 종료 보장 없음(I5). 수렴 규칙 필요: 범위·깊이 명세 고정 + N회 후 "잔여 위험 고지+사람 서명 종결" 전환 | 결함 | **P1** | 열림 (2026-08-26 실기동) |
| 12 | **path-remap 산출에 내용 수준 이동 감지 부재** — 파일 존속·내용 이탈형(2형, I8)은 경로 대조만으로 못 잡음. 계획 단계 산출에 줄수 축소·심볼 이동 스캔 통합(이번 run에서 절차로 선반영, 엔진 fact화는 #4와 묶어) | 결함 | **P1** | 열림 (I8) |
| 13 | **메타스킬(공장) 자체 개선** — 실전 학습 역반영: ① 규칙 문서에 보고 눈높이 규칙(#9) 추가 ② 감사 rubric에 판독 깊이·단위 명세 고정(감사 회차 간 흔들림 축소) | 개선 | P2 | 열림 (om-report 저작 전 반영 권장) |
| 14 | **새 공유 경로 사람 승인 창구 부재** — apply approval인데 awaiting_shared_path_review에서 eligible false 고정, 새 공유 생기는 업그레이드는 verify 도달 불가(구조 공백). 지시서 98→결과서 99, Claude 독립 검증 합격(반례 5종+전체 286 green, digest 봉인 확인), 커밋 3d159b568d | 결함 | **P0** | ✅ 해소 (MR !6 merge, 첫 실전 재주행에서 승인 창구 정상 작동 — S10) |
| 15 | **참조 대상 rename(제3유형) 탐지 부재** — 순수 공식 파일 rename 시 은행 파일의 import가 깨져도 층1 교집합·내용이탈 스캔·apply check 모두 원리적으로 못 잡음(빌드에서야 발견, I11). 처방: "상대 import 전수 해석 스캔"을 계획 산출 필수 절차→엔진 fact로 승격(#4·#12와 통합 후보 — 셋 다 이 스캔 하나로 커버) | 결함 | **P1** | 절차 선반영(5차 재계획), 엔진화 열림 |
| 16 | **build receipt 발행 도구화** — 현행 local-issued는 선언값(dist_source_tree_sha 등)을 발행자가 손으로 적어 정직에 의존, 오래된 산출물 재사용을 못 잡음(I12 실증: 은행 클래스 0건 jar가 receipt 통과 직전). 처방: clean checkout에서 빌드→digest 계산→jar/dist 실물 표본 대조까지 하는 발행 스크립트를 검사기에 편입(81 "local-issued 한계"의 해소) | 결함 | **P1** | 절차 선반영(실물 검증 단계), 도구화 열림 |
| 17 | **apply 성공조건 ↔ verify 시작조건 배타 모순** — verify가 checker HEAD 동일+clean을 동시 요구하나 apply는 관리파일 미커밋 변경을 반드시 남김. 커밋(①위반)·유지(②위반)·원복(digest 위반) 3경로 전부 폐색(엔진 모순 3호, I13). 지시서 100 핫픽스 진행 | 결함 | **P0** | 진행 중 (100) |
