# Codex 지시 — 스킬 2종 2차 적대검토 + 이식 준비 (om-plan·om-verify SKILL.md)

작성일: 2026-08-25. 배경: om-plan·om-verify SKILL.md가 메타스킬 저작→독립감사 PASS→Claude 재검증 승인까지 완료(한글 통일). **당신(Codex)은 구현자 관점의 최종 적대 검토자**다 — Claude와 다른 눈으로, "이 문서대로 따라가면 검사기가 정말 그렇게 동작하나, 우회 여지는 없나"를 코드 실측으로 깨보라.

## 대상 (스킬저장소 `/Users/seop/claude-skill-by-seop` — 읽기, 수정 금지)

- `.claude/skills/om-plan/SKILL.md` (한글, FINAL_STATUS PASS)
- `.claude/skills/om-verify/SKILL.md` (한글, FINAL_STATUS PASS)
- 참고: 저작 증거 `.claude/skill-authoring/{om-plan,om-verify}/`(감사 원문·RULE_COVERAGE), Claude 검토 4건(`검토결과_om-plan_1차_20260825.md`(docs/om-plan/)·`검토결과_om-verify_1차_20260825.md`·`검토결과_om-verify_한글화_확인_20260825.md`·각 INBOX/검토요청)
- 내용 정본: `skill_develop/om_plan/83_...md` 부록 A / 76 / 81 / 24(확정 결정들)

## 검토 1 — 적대 검토 (파일:줄 근거, 호의적 보완 해석 금지)

1. **절차 추적 실측**: 각 SKILL.md의 단계별 명령·인자·종료코드·산출물 서술을 검사기 코드(`work/kb-datacatalog-upgrade-checker-om-plan-cli`)와 전수 대조. 특히 om-plan 7단계(block 재개 — `/om-plan` 재호출→PreToolUse Skill 분기 marker→`plan-resume`)와 om-verify 3단계(exit 지도·pre-run `analysis_error`+3).
2. **우회 시나리오**: SKILL.md 지시를 문면 그대로 따르면서 게이트를 피하거나 거짓 성공을 보고할 수 있는 경로가 있는가(예: run-dir 조작, 요청 필드 생략, exit 재해석 여지).
3. **83 부록 A 재대조**: A-1 스키마 명시·A-2 4단 체크·A-3 케이스/거부 4종·A-4 근거 — Claude 검증과 독립적으로.
4. 알려진 미결과 겹치는 지적은 중복 보고하지 말 것: 3차 이월 6건(om-plan INBOX §5)·한글화 잔여(SPEC_REVIEW 등 증거 문서 잔손질)·hook 필드명 불일치 가능성(이식 후 스모크 테스트로 확인 예정, 아래).

## 작업 2 — 이식 준비 구현 (검토 1에서 차단이 없을 때만, clean-export 작업트리)

대상: `work/kb-datacatalog-upgrade-checker-om-plan-cli` (브랜치 `codex/om-plan-verified-gates-20260820` 위 미커밋 작업만. **커밋·push·MR 금지** — 커밋분리·push는 검증 후 Claude 대행).

1. **이식**: 두 SKILL.md를 `.claude/skills/om-plan/`·`.claude/skills/om-verify/`로 복사(내용 무변경 — 변경이 필요하다고 판단되면 고치지 말고 검토 1 지적으로 반환).
2. **om-apply SKILL.md 한글화**(사람 결정 — 24의 "한글 통일"): 의미 1:1, 기술 토큰 원문 유지 — om-verify 한글화와 같은 규율. 경계·절차·개수 보존을 결과서에 자체 증명(절·표행·경계 항목 수 대조표).
3. **wiring 테스트 보강**(83 작업 3): `harness/tests/test_claude_wiring.py`에 om-plan·om-verify SKILL.md 존재 + 핵심 경계 문구 단언 추가, om-apply 단언은 한글화된 문구로 교체. **단언 문구는 한글**(예: om-verify "재해석하지 않는다"·"통과가 아니다", om-plan "게이트를 우회하지 않는다" — 실제 파일에서 실측해 선택).
4. **전체 테스트 green** 확인(259+ 추가분). 반례 하나 제안: wiring 단언이 실제로 경계 문구 삭제를 잡는지(문구 하나 지운 사본으로 실패 확인 후 원복).

## 이식 후 절차 제안만 (실행 금지 — 사람·Claude 몫)

- **실기동 스모크 테스트**: 검사기 저장소에서 Claude Code 세션을 열어 `/om-plan` 호출 → 훅 발화·marker 생성 확인(공식 문서와 hook_cli.py의 필드명 `slash_command_name` vs `command_name` 불일치 가능성 검증 — 검토결과_om-plan_1차 부수 발견). 결과에 따라 hook_cli 정비 여부 별도 결정.
- om-resume 스킬 파일 신설 여부(24 열린 항목).

## 결과

`skill_develop/om_plan/85_Codex_스킬2차검토_결과_20260825.md`: [검토 1 지적(파일:줄) / 작업 2 반영 위치 / 한글화 보존 증명 / 테스트 결과·반례 확인 / 남은 위험]. 검토와 작업을 섞지 말고 절을 나눌 것.
