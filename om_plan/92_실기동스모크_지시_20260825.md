# 실기동 스모크 테스트 지시서 — 스킬·훅 첫 실전 호출

작성일: 2026-08-25. 수행: **검사기 저장소에서 연 새 Claude Code 세션**. 배경: 스킬 3종(한글)·훅·실행기(harness/om)가 main에 merge됨(MR !2·!3). 문서·정적 검증은 끝났고 **실기동은 0회** — 이 세션이 처음이다. **문제가 나오면 그게 성과다. 억지 통과 금지.**

## 실행 위치·준비

```bash
cd /Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/work/kb-datacatalog-upgrade-checker-om-plan-cli
git pull   # main 최신(스킬 포함) 확인 후 Claude Code 실행
```

주의: 이 저장소의 훅은 `/om-plan` 호출 후 **plan 계열 외 Bash를 기본 거부**한다(설계 동작). 세션이 막히면 그것을 기록하라 — 우회 금지.

## 관찰 목표 (이 순서로)

### S0. 스킬 인식
- `/` 입력 시 om-plan·om-apply·om-verify가 목록에 보이는가. (안 보여도 중단 말고 기록 후 SKILL.md 직접 읽기 경로로 진행)

### S1. 요청서 사전 준비 (보호 시작 전에!)
- `/om-plan`을 치기 **전에** run-request를 만들어 둔다(훅이 걸린 뒤에는 Write가 거부됨 — 이것도 관찰 대상).
- 내용: `mode: change`, `customization_id: BANK-OM-005`, product = `/Users/seop/Documents/Codex/2026-07-24/sites-plugin-sites-openai-bundled/work/om-apply-rehearsal-20260824-02/product-normal`(= GitLab kb-datacatalog와 동일 이력의 로컬본, 시작 기준 8ac18ad053), checker = 이 저장소, registration_path = `harness/registrations/om-temp-1.13.1`. 나머지 필드는 `harness/acgh/plancore/schema/plan-run-request.schema.json` 실측으로 채운다.

### S2. /om-plan 실기동 (핵심)
- `/om-plan BANK-OM-005 수정 계획 run을 시작해줘. 요청서는 <경로>` 입력.
- **관찰 기록(각각 예/아니오/증거)**:
  1. 슬래시 호출이 성립했는가(Unknown command 거부 여부)
  2. **세션 marker가 생겼는가**(훅 상태 디렉터리 실측) — 어느 이벤트로 생겼는지 추정 근거(UserPromptSubmit? PreToolUse Skill 분기?)
  3. 스킬이 SKILL.md 절차대로 갔는가(1단계 확인→`harness/om plan start`→stdout의 state_root/session_id 인용)
  4. plan 외 Bash가 실제로 거부되는가(예: `git status`가 훅에 막히는지 — SKILL.md는 "권한 중단으로 보고"를 지시)
  5. `plan check`까지 가서 **exit 2(approval)를 성공으로 정확히 보고**하는가(재해석 여부)
- 훅 필드명 의심(검토결과 부수발견): marker가 프롬프트 단계에서 안 생기고 Skill 도구 호출 때 생기면 그것대로 기록(설계상 유효 경로).

### S3. 음성 대조 1건
- 새 세션(또는 marker 정리 후)에서 스킬을 거치지 않고 임의 Bash로 등록자료 수정을 시도해 훅이 막는지 1회 확인(실제 수정 금지 — 거부 메시지만 수집).

## 금지
커밋·push·MR·등록자료/제품 수정 · plan run 산출물 조작 · 거부/실패의 우회. 상주 lab 접근 불필요(이 스모크는 계획 단계까지만 — build/서버 없음).

## 결과
`skill_develop/om_plan/93_실기동스모크_결과_20260825.md`: S0~S3 각 관찰의 [결과/증거(명령·출력·파일 경로)/판정], 발견 문제 전부(사소해도), 훅 이벤트 실측 결론(필드명 의심 해소 여부). 이 결과로 파일럿(③) 진행 여부를 사람이 결정한다.
