# CLI 단순화 GitLab MR 병합 전 검토 (2026-08-18)

- MR !1: datacatalog/kb-datacatalog-upgrade-checker, main(0535350) ← codex/om-plan-cli-simplify (원격 69dd83c)
- 검토 방법: GitLab에서 MR 저장소를 직접 clone·checkout, 승인본 dfdc0bc와 대조, clean 단독 시험

## 결론: **병합 가능 → 병합 완료(2026-08-19, main=bd21c14, merge 트리=69dd83c 동일 검증)**

이미 통과 판정한 dfdc0bc가 clean export로 정확히 옮겨졌음을 바이트 단위로 확인.
재구성(test 분리)에 따른 실질 커버리지 손실 없음.

## 확인 결과

1. **구현 무변조** — MR의 om_workflow.py·hook_policy.py·markers.py·test_plan_workflow.py가
   승인본 dfdc0bc와 **blob sha 완전 일치**(4/4). 재패키징이 코드를 바꾸지 않았다.
2. **clean export 범위** — main 대비 코드 5파일만, 문서·md 0. test_om_workflow.py 제외(코덱스
   보고대로 과거 등록자료 의존 — registration_for/common_paths/bootstrap 등, om-plan과 무관).
3. **커버리지 손실 없음** — 승인본 test_om_workflow.py 18개 중 8개가 새 test_plan_cli.py(10개)에
   없으나, 8개 모두 레거시 등록/bootstrap 인자 파싱 테스트다. 유일한 신뢰 관련
   `test_plan_validate_accepts_trusted_expected_digest`는 **인자 파싱만** 검사했고, 그 대상 코드
   (`plan-validate --expected-input-lock-digest`)는 MR에 살아있으며 E안 실질 로직은
   test_plan_cli.py의 test_plan_check_uses_digest_retained_at_start /
   _rejects_digest_override_that_changes_trusted_value / _accepts_external_digest_for_legacy 가 더 강하게 검증.
4. **시험** — clean MR 단독 전체 통과(코덱스 보고 145 passed/2 skipped 재현). skip 2건은
   `/home/user/om-mirror` 미설정 통합 테스트로 타 환경 전용·신규 아님.
5. **P2-1** — Hook의 OM_PLAN_SESSION_ID 자동 전달 미완결, MR 설명에 후속 명시(16번과 일치).

## 남은 것 (병합 후 정리)

- P2-1(후속): hook adapter가 OM_PLAN_SESSION_ID 설정.
- 관찰: MR 저장소가 현재 인증 없이 clone됨(공개 상태로 보임). 병합·안정화 후 접근 정책 확인 권장.
  (검사기 자체가 비밀을 담지 않으므로 P2/비차단이나, 신뢰 루트 저장소이므로 기록.)
