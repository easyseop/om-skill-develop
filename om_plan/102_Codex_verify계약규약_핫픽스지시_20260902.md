# Codex 핫픽스 지시 — verify 계약 실행 규약 2건 (base_url 이중 의미 + 커넥터 계약 앵커)

작성일: 2026-09-02. 사상 첫 계약 테스트 실행(8 fail/1 pass)의 원인 2건 — 둘 다 제품 아닌 검사기·계약 테스트 쪽.

대상: clean-export 작업트리(main 기준). **커밋·push·MR 금지**(Claude 검증 후 대행).

## 원인 A — base_url 이중 의미 (6건 차단, P0)

엔진은 `runtime.base_url`+`/api/v1/system/version`(verify.py:221 — 서버 루트 전제), 같은 값을 OPENMETADATA_BASE_URL로 테스트에 전달(workflow.py:453)하는데 계약 테스트는 `/v1/...`을 붙임(_runtime_contract.py:43 — API 루트 전제). 서버 실측: 어느 값을 줘도 한쪽이 깨지는 완전 모순.

**요구**: 정본 의미를 `runtime.base_url` = **서버 루트**로 확정(엔진 기존 동작 무변경). 엔진이 테스트에 전달하는 `OPENMETADATA_BASE_URL`만 서버루트+`/api`로 조립. `_runtime_contract` 무변경. 이중 `/api` 방지 확인. 반례: ① 서버 루트 입력 → 테스트 env가 API 루트로 전달됨 ② 사용자가 실수로 API 루트를 base_url에 넣으면 기존대로 endpoint 검사에서 정지(완화 금지).

## 원인 B — 커넥터 계약이 1.13.2에서 사라진 파일 형태를 기대 (2건)

_connector_contract.py:149가 `utils/DatabaseServiceUtils.tsx`의 `case DatabaseServiceType.Sybase`(switch)를 기대. 1.13.2에서 선택 로직은 `utils/DatabaseServicePureUtils.ts`의 맵으로 이동했고 은행 커넥터는 그곳에 실재(:104-105, `[DatabaseServiceType.Sybase]: sybaseConnection` / Tibero 동일).

**요구**: 기대 앵커를 새 위치·맵 형태로 갱신. **검사 의도 불변**(두 커넥터가 선택 로직에 실재 + 스키마 왕복). 근거는 승인 계획의 U8 path-remap(내용 이탈 재귀속, 사람 승인) — 범위·소유자 변경 아님. 반례: ① 맵에서 Sybase 한 줄 제거된 트리 → fail ② 옛 위치(switch)에만 있는 트리 → fail(1.13.1형 회귀 방지) ③ 정상 1.13.2형 → pass.

## 공통

전체 테스트 green. 이번 8건 실패 중 6건은 원인 A에 가려 실제 동작 미검증 상태였음 — 수정 후 실행에서 진짜 결과가 처음 나온다는 점을 결과서에 명시. 결과서: `103_Codex_verify계약규약_결과_20260902.md`.
