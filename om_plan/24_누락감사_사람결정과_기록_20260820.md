# 누락 감사(22) 후속 — 사람 결정 항목 + 기록

작성일: 2026-08-20. 근거: `22_누락_감사_결과_20260820.md`. 기존 Q1~Q9는 `06_남은_결정과_주의점.md` 참조.
원칙: 아래 신규 결정은 Claude·Codex가 임의 확정하지 않는다. 선택지·권고로 사람에게 올린다.

> ★★★ **최우선 취급**: 이 문서(결정기록)와 아래 "재검토 대상(임시 결정)" 섹션은 프로젝트에서 **가장 중요하게** 다룬다. 임시로 내린 결정은 **반드시 나중에 재검토**한다 — 임시 결정이 그대로 굳어 정본이 되는 것을 막는다. 새 세션·다른 에이전트는 이 문서부터 확인한다.

## ★ 재검토 대상 (임시 결정 — 반드시 나중에 재검토)

> 여기 있는 항목은 "지금은 이렇게 가되, 조건이 열리면 다시 판단한다"는 **임시** 결정이다. 굳히지 말 것.

| # | 임시 결정 | 재검토 트리거(언제 다시 보나) | 상세 위치 |
|---|---|---|---|
| R-1 | A-3 기준선 잠금은 **릴리즈 때만 전진**, 동시작업은 별도 브랜치 | verify 단계 착수 / apply 개방 시 (릴리즈 정의·잠금 저장위치·전진 트리거 재검토) | 아래 "확정된 결정"의 A-3 항목 |
| R-2 | 지금은 **A′(검사기만: 테스트 누락 방지)** 만 하고 A(기준선 잠금)는 apply 개방 후로 미룸 | apply·GitLab 반영 보류 해제 시 A 얹기 | 같은 A-3 항목 |
| R-3 | ✅ 해소(옵션3): A-4가 direct_tests를 tests에서 제거. **단 매니페스트 `assurance.direct_tests`는 死필드로 잔존** | 스키마·등록자료 정리를 몰아서 할 때 死필드 제거 검토 | D-directtest / 지시서 48 |
| R-4 | 등록 밖 변경 커버리지 게이트가 `registration_prep`·`vendor_rebuild`엔 있으나 **/om-plan에 자동 배선 안 됨** | Q21 배선 결정 + implement/verify 착수 시 | Q21 / doc 55 |
| R-5 | 재베이스(Q10)는 실제 candidate tree 필요 → /om-plan 단독 불가, 단계 A~D 미정 | Q10 사람 결정 + implement 착수 시 | Q10 / doc 55 |
| R-6 | om-verify V-1~V-3(환경 C안·UI 핵심부터·콘솔 기본fail) — **임시 확정** | om-verify 실물 운용 후 (비율·범위·수위 조정) | V-1~V-3 / doc 73 |


## 확정된 결정 (2026-08-20 사람 결정)

- **Q9 (기존, 06) — 확정: exit 2(approval)를 CI 성공으로 처리.** 근거: `/om-plan`은 아무것도 반영하지 않으므로 초록불이어도 위험 없음. 미해결 항목(담당자 등)은 뒤 단계(apply의 owner_status 게이트·verify·최종 배포 승인)에서 강제됨. 사람이 매 계획을 다 볼 수 없고 이후 verify가 검증하므로 계획 단계에 사람 정지 게이트를 두지 않는다. → Codex: CI wrapper가 exit 2 → CI 성공(가능하면 "review-ready" 상태 표기).
- **Q11 — 확정: 업그레이드 산출물 검증을 3층으로.** (기준 = 01A §4의 9개 산출물 = 체크리스트 CHK-1~9. watch는 제외 = 별도 게이트.) 초기 "존재만 강제" 안을 적대 검토로 폐기하고 아래로 격상. 이유: "존재+N/A+자기신고 라벨"은 checkbox 극장(가짜 사유로 N/A·아무 라벨 부착 가능)이라 위장만 만든다.
  - **층1 [검사기 결정론 — 못 속임]**: 사실-근거 커버리지 강제.
    - `path-remap`: 영향 경로(`official-upgrade-paths ∩ 등록 커스텀 경로`)를 **전부 커버**해야 함(shared_impact처럼 자동 교집합). 빠진 경로 → block.
    - `doc-code-crosscheck`: `official-doc-findings`의 각 항목이 crosscheck에 연결돼야 함(operations의 finding_id 연결 방식).
  - **층2 [LLM 2차 판독 — 검사기 아님]**: `official-doc-findings`·`operations`의 **문서 누락은 뒤 테스트가 못 잡으므로**(코드 아닌 배포 요구사항), 원문 snapshot을 **독립 LLM(별도 agent)이 다시 읽어** findings와 대조. 누락 발견 시 질문/차단. 사람 병목 없이 자동, 단 LLM이라 ground truth는 아님(누락 위험을 줄이되 없애지 못함 — 정직 고지).
  - **층3 [존재만]**: `upgrade-plan` 등 판단형 산출물은 비어있지 않은 존재만.
  - `checklist_ref`: 관리 파일 변경에 구조화 필드(`checklist_ref: [CHK-n]`)로 표기하되 **자기신고라 검증 강도 낮음(참고용)**. 실제 강제는 층1 커버리지.
  - 근거: 코드 산출물은 뒤 테스트가 잡지만(사람검토 불요), **문서 완전성은 어떤 뒤 테스트도 안 잡아 운영에서 터짐**(05 문서 한계) → 층2로 메움.
  - → Codex 지시서(23) 조치 3, upgrade.md에 반영.

- **P1-1 / changed_path_count 스코프 — 확정: 원본(source) 기준.** (2026-08-20 사람 결정) 등록 파일의 `source.changed_path_count`는 **원본 스냅샷 기준**으로 재고(현재 HEAD 기준으로 재계산하지 않는다). 이는 두 가지를 뜻한다: (1) 제 원래 갭 A5 지적("count가 current-diff-paths.txt와 어긋난다")은 **오진** — 두 값은 스코프가 달라 원래 달라야 정상. (2) Codex의 A5 조치(apply의 count current-재계산 + `validate_current_diff_count`)는 **되돌린다**. count는 기존대로 `source-diff-paths.txt`(=`build_reconstruction_plan` 불변식) 기준을 유지. → Codex 지시서 23 조치4를 "current 정합"에서 "A5 되돌리기"로 갱신. 큰 Q10(upgrade 재베이스 시 source 블록 갱신)은 별개로 열림.

- **A-3(change 동작변경 판정) / 기준선 잠금 정책 — 확정(임시): 잠금은 릴리즈 때만 전진.** (2026-08-20 사람 결정)
  - **배경**: change 검사에서 "동작이 바뀌었나"를 자동 판정하려면 비교 시작점(custom_baseline)을 믿을 수 있어야 하는데, 현재 custom_baseline은 `request["refs"]`로 들어와(`preflight.py:150-153`) 등록부의 어떤 잠긴 SHA와도 대조되지 않는다(운영자 저작=조작 가능). 또 등록부 `snapshot_sha`는 최초 등록 스냅샷일 뿐이고 apply가 전진시키지 않으며(`registration_prep.py`에서 read-only, 400·805줄), registry는 `registry_after`로 통째 덮여(871줄)·쓰기 화이트리스트 대상(964줄)이라 source 블록도 제안서로 바뀔 수 있다.
  - **결정한 것**: 신설할 "마지막 승인 커스텀 SHA" 잠금은 **릴리즈 시점에만 전진**한다(매 apply마다 아님). 중간 수정들은 전부 직전 릴리즈 기준으로 비교된다 → "직전 승인마다 잠금 이동" 시 생기는 뻑뻑함(릴리즈 대비 전체 검토가 block되는 문제)을 회피. 동시 작업은 **별도 브랜치**로 처리(잠금은 릴리즈 기준이라 병렬 브랜치가 서로 안 막음).
  - **임시성**: 이 결정은 **임시**다. verify 단계·apply 개방 시 재검토 대상(예: 릴리즈 정의, 잠금 저장 위치·전진 트리거).
  - **실행 순서(중요)**: 기준선 잠금(A)은 **apply를 고쳐야** 성립(잠금 칸 신설 + 릴리즈 시 전진)하는데 apply·GitLab 반영은 현재 보류 영역이라 **지금은 못 끝낸다**. 그래서 **지금은 검사기만 고치는 A′**(그 커스텀에 등록된 테스트가 `contracts-to-run`에 반드시 존재 — 없으면 block. 관측·동작판정은 안 함)를 먼저 하고, apply 개방 시 그 위에 A(기준선 잠금 + custom_baseline==릴리즈 잠금 강제)를 얹는다. A′와 A는 겹치지 않아 A′ 먼저 해도 A가 버려지지 않는다.
  - **판정 자체의 한계(불변)**: "동작 바뀜/보존"의 자동 판정은 계획 단계에선 원천적으로 불가(양쪽 입력이 운영자 저작). 실제 판정은 verify(실행)+사람. A′는 판정이 아니라 "테스트 누락 방지"만 한다.

- **리허설 P0-3 / 활성 공유소유맵 정정 — ✅ 확정(사람 승인, 2026-08-24).** om-apply 실데이터 리허설(doc 66)이 찾은 기존 등록자료 불일치(매니페스트 파생 공유 39 vs shared-path-owners 37) 정정 승인: 누락 2경로(serviceConnection.ts·DatabaseServiceUtils.test.tsx)를 활성 `shared-path-owners.yaml`에 추가(소유자는 매니페스트 실측 기입). → 지시 67. 등록자료 digest 변동은 정상.
- **리허설 P0-2 / 활성 기준(candidate) 잠금 부재** — 신규 항목으로 만들지 않고 **기존 R-1·R-2(기준선 잠금, 릴리즈-only) 재검토 때 함께 처리**. 실증: 엉뚱한 custom_baseline(59dae915)로도 plan approval 재현(doc 66 §8 P0-2). commit-inventory가 provenance인지 active lock인지 그때 명시.

- **test-agent 채택 — ✅ 확정(사람 결정, 2026-08-24).** om-verify의 **UI층 부품**으로 채택(본체=pytest 계약 실행과 분리). 조건 구조: **정형 I/O 계약(입력 YAML/출력 report.json·JUnit·종료코드) 뒤의 교체 가능한 부품** — 문제 시 본체 변경 없이 교체. 근거: OM lab 파일럿 통과(doc 72 — 세션·SPA·대조 17=17·실전 결함 검출). 도구 채택 공통 기준으로 "정형 I/O로 통제 가능한가"를 항상 평가한다(사용자 지시).

## 신규 사람 결정 (임의 확정 금지)
### V-1~V-3 — om-verify 설계 사람 결정 — ✅ 확정(임시, 2026-08-24)
- **V-1 = C(혼합)**: 평시엔 상주 lab에 후보 배포(빠른 회귀), 릴리즈 관문은 매회 fresh 빌드(완전 격리). 어느 쪽이든 "떠 있는 서버=진짜 candidate" 기계 결속 필수.
- **V-2 = 핵심 화면부터 + API 정답원**: UI 시나리오는 핵심 커스텀(기준코드 등) 1~2개로 시작해 단계 확대. 정답원은 API 우선(백엔드는 계약 테스트 담당).
- **V-3 = 기본 fail + 합의 예외 목록**: 콘솔·HTTP 오류는 기본 실패, "정상으로 합의된 노이즈"만 명시 목록 제외(파일럿 검증 방식). instanceCode류는 upgrade 재적용 후 소멸해야 정상 → 회귀 지표.
- **셋 다 임시** — 실물 운용하며 재검토(아래 R-6).


### D-directtest — registered-tests와 direct_tests 비대칭 (A′ 구현 중 발견, 2026-08-20)
- **문제**: A-4 fact `customization-relations[X].tests`는 매니페스트 `assurance.direct_tests`를 포함하나, 기존 flat fact `registered-tests`는 **Contract의 `required_tests`만** 포함한다. 그래서 contract에 없는 direct-only 테스트를 proposal이 `status: existing`으로 표기하면 기존 검사(`collectors.py:600` "test claimed as existing is not registered")가 **거짓 block**한다. A′는 그 테스트를 실행목록에 넣도록 요구하므로 둘이 충돌 가능.
- **현황**: 1.13.1 실데이터엔 `assurance.direct_tests` 항목이 **하나도 없어** 지금은 발현 안 됨(잠재). Claude 데이터 확인 완료(direct-only=NONE). **방향은 false-BLOCK(과잉차단)** 쪽이라 위험(false-pass)은 아님 — 안전 실패.
- **선택지**: (1) `registered-tests`에 direct_tests도 포함(Codex·Claude 권고 — 제안자·검사기 해석 일치). (2) direct-test 전용 status 의미를 문서·검사기에 명시. (3) A-4 관계 fact의 tests를 contract-tests로 축소.
- **선택지 검토 후 권고 변경**: 처음 (1)을 권고했으나, direct_tests는 계약을 우회하는 통로이고 실데이터에서 전부 `[]`(아무도 안 씀)임을 확인. 통로를 하나(계약)로 정리하는 (3)이 더 깔끔.
- **✅ 확정(사람 결정, 2026-08-20): 옵션(3).** A-4가 매니페스트 `assurance.direct_tests`를 관계 fact `tests`에 union하던 것을 **제거** → 관계 `tests` = 그 ID 계약 required_tests만. `registered-tests`(계약 테스트)와 의미 일치해 §7 소멸. 매니페스트 필드는 死필드로 남김(등록자료 미변경). → Codex 지시서 `48_Codex_directtest제거_지시_20260820.md`. 결과 49에서 Claude 재검증.



### Q10 — upgrade 반영 경로 (갭 A4)

- 문제: 정책 §6은 "upgrade도 plan→승인→apply"라 하나, 구현된 apply(`registration_prep.py`)는 `source` 블록(`upstream_sha`/`upstream_tag`/`snapshot_sha`/`changed_path_count`)을 갱신하지 않는다. 등록 폴더는 버전별(`om-temp-1.13.1`)이라, 새 공식 버전의 등록 `source`를 **누가 만드는지** 정해져 있지 않다.
- 선택지:
  - (A) upgrade 반영을 apply가 하되, apply가 새 버전의 `source` 블록을 갱신하도록 확장.
  - (B) 새 공식 버전은 **새 등록 폴더를 bootstrap**으로 생성(기존 커스터마이징을 새 버전 위로 이관). apply는 동일 버전 내 변경만.
  - (C) 혼합: 커스터마이징 이관은 plan/apply, 버전 기준선 갱신은 bootstrap.
- 무엇을 막나: 이 결정 전에는 실제 upgrade 반영을 실행할 경로가 닫혀 있다.
- 권고: 정보 부족. 사람 결정 필요.

- **★ Codex 코드조사(2026-08-21, 질의응답 doc 55) 반영 — 재베이스 단계 선택지 구체화:**
  - **핵심 사실**: `/om-plan upgrade`만으로는 재베이스를 **확정할 수 없다.** 요청서가 잠그는 ref는 `official_base·official_target·custom_baseline`뿐이고 **"재베이스된 최종 candidate ref"가 입력에 없다**(plan-run-request.schema.json:125-133). 새 `snapshot_sha`·전체 diff·`changed_path_count`·등록 밖 잔재는 **실제 재베이스 tree가 생긴 뒤에만** 계산 가능. 최초등록 선례(initial_registration.py:660-675)도 실제 SHA 2개가 있어야 source를 계산함.
  - **재베이스 단계 선택지 (Q10, 사람 결정)**:
    - Q10-A plan-only: 현재 입력구조로 **최종 source 확정 불가** → candidate 입력·post-implement plan 없이는 제외 후보.
    - Q10-B implement에서 계산+기록: 실제 계산 가능하나 **R-1(릴리즈-only 전진)과 자기검증 위험** → 활성 기록 분리 필요.
    - Q10-C apply에서 처음 계산+기록: 원자적이나 **승인 전 검토자료가 없음**(늦은 발견).
    - **Q10-D 분리형 (Codex 권고)**: pre-plan 제안 → implement가 candidate 생성 → post-implement reconcile이 source 재베이스안 계산·검증 → 사람 승인 → apply가 승인값 한 번만 기록. R-1·P1-1과 정합(`changed_path_count`=새 source inventory 행수, vendor_rebuild.py:99-106 불변식). **단 사람 결정 전 미확정.**
  - `custom_baseline`은 run 입력으로는 잠기나 **등록 `source.snapshot_sha`와 교차 대조 없음**(둘이 달라도 approval — test_plan_workflow.py:1235-1257). 기준선 잠금 A(R-1) 구현 시 이 교차게이트를 넣어야 함.

### Q21(신규) — 기존 커버리지 게이트를 /om-plan에 배선할지 (등록 밖 변경)
- **★ Codex 조사(doc 55) 정정**: "등록 밖 변경(코드엔 있는데 미등록 경로)을 막는 게이트가 전 단계에 없다"는 **틀림.** 실측 결과:
  - `/om-plan plan check` 단독: **일반 커버리지 게이트 없음**(맞음). upgrade path-remap(공식변경∩등록경로)·dirty-worktree만 부분 장치. `net-custom-paths`는 판정부 미소비.
  - `registration_prep.build_plan`: **`UNOWNED_NET_CHANGE` block 있음**(507-519) — 최종 net 경로가 단일-ID commit에 소유 안 되면 차단.
  - `vendor_rebuild.py`: **강한 커버리지 게이트 있음** — 모든 inventory 경로가 Manifest 소유 또는 명시적 제외(`unregistered_findings`)여야 함(181-217), inventory==실제 Git diff(280-320), reconstructed candidate 검증(459-645).
  - **단 이 게이트들은 `/om-plan upgrade`·`apply_plan`에 자동 내장이 아님**(별도 명령 vendor_rebuild plan/verify·bundle validate에서만 실행). "코드에 있다"≠"매번 자동 실행".
- **결정 필요(사람)**: 이미 있는 `registration_prep`·`vendor_rebuild` 커버리지 검사를 **`/om-plan`의 어떤 완료조건으로 묶을지**(=배선). 새 게이트 신설이 아니라 배선 결정. → 재검토표에도 등재.

### Q11 — upgrade 필수 산출물의 검사기 강제 여부 (갭 A3) — ✅ 확정: 3층(커버리지+2차판독+존재), 위 "확정된 결정" 참조

(아래 (A)/(B)는 초기 논의 기록. 적대 검토로 "존재만"은 폐기되고 3층으로 격상됨.)

- 문제: 01A는 upgrade 출력으로 path-remap·doc-code-crosscheck·operations-checklist·shared-code-delta를 규정하나 검사기는 존재를 강제하지 않는다(path-remap 검증 0줄). 지금은 LLM 책임(upgrade.md에 명시).
- 선택지:
  - (A) 검사기가 mode=upgrade일 때 path-remap 등 필수 산출물 존재를 강제(Codex 조치 3).
  - (B) 강제하지 않고 LLM 책임 + 사람 검토로 둔다(현행). 05 문서가 "자동 검증 범위는 findings→checklist 연결까지"라 한 한계와 정합.
- 무엇을 막나: 없음(현행도 동작). 품질 보증 강도 결정.
- 권고: 최소 path-remap만이라도 (A) 검토. 나머지는 (B) 유지 가능.

## 기록 (결정 아님 — 추적성/주의)

### 기록-1: intent_review 게이트 (갭 A8 추적성)

- 사실: `preflight.py:316`가 `intent_review_required: true`를 설정하고 `ci/om_plan_ci.py:147`가 강제한다. 승인(approval) 전 사람이 의도를 확인하도록 하는 게이트다.
- 추적성: 설계 문서(00·01·01A·02·03·04·05·검토서)에 "intent" 문자열이 없다. **설계에 명명되지 않고 구현 단계에서 추가된 게이트**다. 기능은 안전측(사람 확인 강제)이라 무해하나, 설계-구현 추적성이 결여됐음을 여기 기록한다. (01A의 "approval=계획 검토 준비"·사람 STOP 취지와 정합)

### 기록-2: risk/patch-kill의 사람 입력 대기 (갭 A6)

- 사실: `risk`·`patch-kill` 명령은 사람이 작성하는 입력 파일(`change-intent.yaml`·`patch-kill-plan.yaml`)이 없으면 `require_file` 가드로 즉시 fail-closed한다(`om_workflow.py:685,768`).
- 판정: **설계된 동작**(사람 입력 대기)이며 버그 아님. 이 명령들을 쓰려면 사람이 먼저 해당 입력을 작성해야 함을 기록한다.

### 기록-3: 등록 파일 개정 이력 (이전 Q10안 해소)

- 앞서 "등록 파일 개정·이력 관리 방식"을 새 결정으로 올릴지 검토했으나, 22 감사 결과 **애초에 이력/버전 전이 메커니즘이 설계에 없고**(provenance는 출생경위만, 불변), 대신 `management-files.md`에 **필드별 변경 조건**을 실제 동작대로 문서화하는 것으로 갈음했다. 별도 결정 항목으로 올리지 않는다.

### ✅ 확정(2026-08-24): apply·verify 일괄 커밋분리 + GitLab 반영 진행

- 사람 결정: "verify까지 개발·테스트를 마쳤으니 커밋분리하고 스킬(도구)을 GitLab에 push한다." om-report(4단계)는 그 다음.
- 반영: clean-export 브랜치에 3커밋 추가(등록자료 P0-3 정정 → om-apply v1 → om-verify v1, 결과서 82). GitHub=원천·GitLab=기능(도구) 원칙 유지.
- 주의(진행 조건): GitLab 파이프라인은 om-plan 전용이라 apply/verify 테스트를 돌리지 않음 — CI 확장 여부는 별도 검토 항목(82 참조).

### ✅ 확정(2026-08-24): 스킬 문서 저작 방식 — 메타스킬(claude-skill-author) + Codex 검토

- 사람 결정: 스킬저장소(claude-skill-by-seop)에 claude-skill-author 패키지(v1.1, 표준문서 01·02 내장)를 설치하고, **새 Claude 세션이 그 절차(설계서→spec→작성→build→독립감사→FINAL_STATUS)로 스킬을 저작 → Codex가 검토**한다. 스킬명은 예시(omplan)가 아니라 `om-plan`으로 통일.
- 83과의 관계: 83은 "Codex가 작성" 지시였으나 **작성 주체가 변경**됨. 83 부록 A(I/O 정본·4단 체크·케이스별 지시)는 내용 요구사항 정본으로 유지 — 저작 지시서(스킬저장소 `지시서_om-plan_스킬저작_20260824.md`)가 이를 참조.
- 검증 기록: 패키지 자체 테스트는 macOS 심링크 TMPDIR에서 경로비교 불일치로 실패 → 심링크 없는 TMPDIR에서 전부 통과(환경 요인 확정, 패키지 P2 버그로 기록·제작처 피드백 대상). install.sh는 .claude/ 한정·백업 생성·네트워크/Git 쓰기 없음 실측.
- 재검토: om-plan 저작 결과를 보고 om-verify 등 후속 스킬에도 같은 방식을 쓸지 결정.

### ✅ 확정(2026-08-25): om-plan 스킬 재감사 한도 예외 1회 승인

- 배경: 메타스킬 규정상 수정·재감사 최대 2회 — om-plan 저작이 3차 CONDITIONAL(차단 2건: plan-resume 값 출처 관측 불가 / 미등록 `/om-resume` 의존)로 한도 소진.
- 사람 결정: 사용자 "고치고 한 번 더 검사받아"(2026-08-25) — 차단 2건+비차단 3건 수정 후 **4차 독립 감사 1회 예외 승인**. 수정 적용은 1차 검토 세션(Claude), 감사는 새 독립 인스턴스(작성자·감사자 분리 유지).
- 차단 B의 판정 근거: 공식 문서 조사로 "파일 정의 없는 슬래시 명령은 훅 발화 전 거부" 확정 → 재개 방아쇠를 `/om-plan` 재호출(PreToolUse Skill 분기, hook_cli.py:123-133)로 교체.
- 열린 항목(후속 결정): om-resume 스킬 파일 신설 여부(이식 시) / om-verify 본문 영어 유지(D-01, 유지 권고) / hook 필드명(command_name vs slash_command_name) 불일치 가능성 → **이식 후 실기동 스모크 테스트 필수**(검토결과서 근거).

### ✅ 확정(2026-08-25): 스킬 본문 언어 한글 통일

- 사람 결정: "한글로 통일하는 게 좋긴 한데"(2026-08-25) → 한글 통일 채택. 근거: 사내 GitLab에서 한국어 조직이 직접 읽는 도구 문서 + 작성표준 D-01(규칙 문장 한글) + om-plan 기저작본과 일관.
- 실행: om-plan 그대로(이미 한글) / **om-verify 한글 재빌드+새 감사**(스킬저장소 `지시서_om-verify_한글화_20260825.md`) / **om-apply 한글화는 Codex 단계**(wiring 테스트 om-plan·verify 단언 추가(83 작업3)와 함께, 단언 문구도 한글로).
- 유의: D-01 예외(영어) 결정은 이 결정으로 대체됨. 앵커 70개 재작성 비용 인지하고 승인.

### ✅ 확정(2026-08-25): feature 신규 ID는 사람이 요청서에 필수 기재

- 배경: Codex 2차 검토(85)의 P0-1 — feature 요청에서 `customization_id` 생략 시 중복 검사가 공회전해 기존 ID 재사용 제안이 approval 도달(실측 반례). Claude 재검증으로 확정(스키마 feature 절에 ID 비필수 + collectors.py:550 요청값만 대조).
- 사람 결정: **옵션 1 — 사람이 요청서에 새 ID를 필수 기재**(안 적으면 즉시 거부, 적으면 기존 등록 ID와 결정적 중복 검사). 근거: ID는 관리 체계의 열쇠 — 사람 채번이 "사람=결정자·검사기=결정적 판정" 철학과 정합, 구현·검증 부담 최소.
- 동반: P0-2(인터프리터 부재/구버전·스킬 간 표기 불일치)는 기술 권고대로 **저장소 관리 실행 래퍼**로 통일(계약은 지시서 86).

### ✅ 확정(2026-08-26): 업그레이드 계획의 2차 판독 16건 처리 방침

- 계획반영 6건(IDR-001·002·011·013·015·016) 승인 — 근거: 세션 판단표 + Claude 교차검토(등록자료 실측 정합).
- 운영 질문 10건은 **랩 기준 일괄 처리**: "검증 랩 환경 미사용/해당없음"으로 계획에 명시 판정하고, **실운영 반입 시 재확인 체크리스트**로 이관·기록. (등록자료는 코드를 기록하지 운영 방식을 기록하지 않으므로 구조적으로 장부 근거가 없는 항목들)
- 부수: 2차 판독의 전제 오류 1건(IDR-010 — 은행이 수집기 코드를 보유한다는 가정)은 세션이 실측으로 정정, 판독 원문은 불변 유지(판독은 ground truth 아님 원칙).
