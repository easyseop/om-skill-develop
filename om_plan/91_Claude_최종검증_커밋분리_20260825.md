# Claude 최종 검증 — 이식 결과(90) + 커밋분리 + GitLab 반영

검증일: 2026-08-25.

## 1. 90 재검증 — 통과

- **이식 digest 재현**: om-plan(3bdd7965…)·om-verify(b43c56a7…) 원본=대상 일치, 커밋본에서도 재확인.
- **om-apply 한글화 의미 대조**: git 이력의 영어 원본(51줄, 커밋 093c5ac8b1)과 직접 대조 — 경계 4·절차 6·기술 토큰·STOP/scope_variances/3단 복구 규율 전부 보존, 명령 `harness/om apply start/check` 통일. 완화 0.
- **wiring 7종 + 삭제 반례 내 재실행 통과**. 전체 테스트 내 재실행 **278 passed·exit 0**.
- 변경 범위 = 87(10) + 90(4) 정확히 일치.

## 2. 커밋분리 — 3커밋 (혼합 파일 hunk 분리 포함)

| 커밋 | 내용 | 시점 테스트 |
|---|---|---|
| `058181067b` | P0-1: feature customization_id 필수화 + 제안 대조 + 반례 6종 | 268 green |
| `1228db92c8` | P0-2: harness/om 실행기 + 훅 정합 + 문서 (test_plan_workflow 훅 hunk 3개 이쪽) | 276 green |
| `b5e80851f0` | 스킬 3종 이식·한글화 + wiring 한글 단언·삭제 반례 | 278 green |

- 누적 diff = 분리 전 기준선과 **완전 동일**(추적 9파일 699줄 diff 대조), 작업트리 clean.
- 테스트 수가 87(276)·90(278) 문서 기록과 계단식 일치.

## 3. GitLab 반영

- **발견: MR !2는 이미 사람이 merge** — 기존 6커밋(om-plan 3 + 등록정정·apply·verify 3)이 main에 반영됨, 원격 브랜치 자동 삭제 상태였음.
- 새 3커밋으로 브랜치 재생성 push → **MR !3 생성**(target=main). merge는 사람 몫.
- PAT 사용(파일 기록 없음).

## 4. 다음 (사람·Claude)

1. MR !3 검토·merge — 사람.
2. **실기동 스모크 테스트**: 검사기 저장소에서 Claude Code 세션 열어 `/om-plan` 호출 — 훅 발화·marker·명령 흐름 확인(훅 필드명 의심 검증 포함).
3. **스킬 경유 파일럿**: BANK-OM-005 데이터로 `/om-plan`→`/om-apply`→`/om-verify` 실제 운전 — "스킬로 확실히 되나"의 최종 증명.
