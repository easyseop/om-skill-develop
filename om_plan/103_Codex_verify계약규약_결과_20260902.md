# Codex 결과 — verify 계약 실행 규약 핫픽스

- 작업일: 2026-09-02
- 지시서: `102_Codex_verify계약규약_핫픽스지시_20260902.md`
- 작업 트리: `work/kb-datacatalog-upgrade-checker-om-plan-cli`
- 브랜치: `hotfix/verify-start-condition-20260902`
- 시작 HEAD: `66561d449f8af623e928fc33596b9b2dae3abd93`
- 상태: 작업 트리만 수정. commit·push·MR 없음

## 1. 반영 결과

### 원인 A — 서버 루트와 API 루트의 역할 분리

- 반영 위치: `harness/acgh/verifycore/workflow.py:453-455`
- `runtime.base_url`의 의미는 지시대로 **서버 루트**로 유지했다.
- 엔진의 endpoint 확인 방식인 `server_root + /api/v1/system/version`은 변경하지 않았다.
- 계약 테스트에 넘기는 `OPENMETADATA_BASE_URL`만 `server_root.rstrip("/") + "/api"`로 조립한다.
- `tests/bank/contracts/_runtime_contract.py`는 수정하지 않았다.

결과적으로 서버 루트가 `http://runtime/`이면 계약 테스트에는 `http://runtime/api`가 전달된다. 사용자가 API 루트인 `http://127.0.0.1:8585/api`를 잘못 입력하면 엔진이 기존 방식대로 `/api/api/v1/system/version`을 조회하다 endpoint 검사에서 중단한다. 잘못된 입력을 자동 보정하는 완화는 추가하지 않았다.

반례 위치:

- `harness/tests/test_om_verify_counterexamples.py:174-195` — 서버 루트 입력이 정확히 한 번 `/api`가 붙은 테스트 환경값으로 전달됨
- `harness/tests/test_om_verify_counterexamples.py:567-599` — API 루트 오입력은 endpoint 검사에서 `VERIFY_ENDPOINT_UNREACHABLE`로 중단됨

### 원인 B — 1.13.2 커넥터 선택 앵커로 갱신

- 반영 위치: `tests/bank/contracts/_connector_contract.py:58-79`, `168-172`
- 옛 `DatabaseServiceUtils.tsx`의 switch 문 검사를 제거했다.
- 1.13.2의 `DatabaseServicePureUtils.ts` 안 `databaseSchemaMap`에서 아래 대응을 검사한다.
  - `DatabaseServiceType.Sybase` → `sybaseConnection`
  - `DatabaseServiceType.Tibero` → `tiberoConnection`
- 단순 파일 전체 문자열 검색이 아니라 `databaseSchemaMap` 블록 내부의 정확한 map entry를 검사한다.

기존 검사 의도는 유지했다. JSON 스키마 유효성·payload 왕복·databaseService 참조·생성 타입과 enum·`getDatabaseConfig` 단위 테스트·아이콘 파일 검사는 제거하거나 완화하지 않았다.

반례 위치: `harness/tests/test_connector_contract_anchors.py:30-80`

- 정상 1.13.2 map 형태: pass
- Sybase map entry 누락: fail
- 옛 switch에만 존재하고 새 map에는 없음: fail

## 2. 실제 1.13.2 코드 확인

제품 복사본 `/Users/seop/om-work/om-1.13.2-build`을 대상으로 Sybase·Tibero 계약을 실행했다.

```text
tests/bank/contracts/test_sybase.py
tests/bank/contracts/test_tibero.py
→ 2 passed
```

두 커넥터가 실제 1.13.2 `DatabaseServicePureUtils.ts`의 `databaseSchemaMap`에 등록된 형태로 통과했다. 이 결과는 정적 커넥터 계약의 앵커 갱신이 실제 제품 트리와 맞는다는 뜻이며, 실서버 API 동작까지 통과했다는 뜻은 아니다.

## 3. 검증 결과

| 구분 | 결과 | 확인한 내용 |
|---|---:|---|
| base URL 집중 반례 | 2 passed | 서버 루트 변환, API 루트 오입력 중단 |
| 커넥터 앵커 반례 | 3 passed | 정상 map 통과, 누락·옛 switch-only 차단 |
| verify 반례 + 커넥터 앵커 | 29 passed | 기존 verify 차단 규칙 회귀 없음 |
| 실제 1.13.2 Sybase·Tibero 정적 계약 | 2 passed | 새 map 앵커와 기존 스키마 왕복 검사 |
| 전체 테스트 | 296 passed, 10 skipped | 전체 회귀 실행 완료; skip은 pass로 합산하지 않음 |
| `git diff --check` | pass | 공백·patch 형식 오류 없음 |
| 변경 Python compileall | pass | 문법 오류 없음 |

## 4. 지시서 수용 기준 대조

- [x] `runtime.base_url`은 서버 루트로 유지
- [x] 계약 테스트 환경에만 서버 루트 + `/api` 전달
- [x] 이중 `/api`를 정상 입력에서 만들지 않음
- [x] API 루트 오입력은 endpoint 검사에서 중단
- [x] `_runtime_contract.py` 무변경
- [x] 커넥터 선택 앵커를 1.13.2 map 형태로 변경
- [x] Sybase 누락·옛 switch-only·정상 1.13.2 반례 추가
- [x] 스키마 왕복 등 기존 계약 의도 유지
- [x] 전체 회귀 실행
- [x] commit·push·MR 없음

## 5. 변경 파일

```text
M  harness/acgh/verifycore/workflow.py
M  harness/tests/test_om_verify_counterexamples.py
M  tests/bank/contracts/_connector_contract.py
?? harness/tests/test_connector_contract_anchors.py
```

제품 코드·활성 등록자료·`SKILL.md`·references는 수정하지 않았다.

## 6. 남은 위험과 다음 확인

이전 실서버 실행의 `8 fail / 1 pass` 중 6건은 원인 A 때문에 의도한 API endpoint에 도달하지 못했다. 따라서 그 6건은 기능 실패가 아니라 **실제 동작이 아직 검증되지 않은 상태**다.

이번 작업에서는 실서버 9개 계약을 다시 실행하지 않았다. 단위·반례·정적 계약이 통과했더라도 실서버 기능 통과로 단정할 수 없다. 이 핫픽스 이후의 다음 실서버 실행이 6개 계약의 첫 유효 기능 결과가 된다.

다음 단계에서는 서버 루트를 `runtime.base_url`로 입력하여 9개 계약을 새 run에서 실행하고, 각 계약의 pass·fail·skip을 그대로 기록해야 한다. 그때 남는 실패부터 제품 기능·fixture·계약 정의 문제로 분리해 판단한다.
