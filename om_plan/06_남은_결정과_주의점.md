# 남은 결정과 주의점 (2026-08-14 2차 재검토 시점)

## 사람 결정 대기 — Q1~Q9 (`06_구현전_사람결정_질문목록.md`)

구현 착수 전 **반드시 답해야 하는 4개**:

| # | 질문 | 권고안 | 비고 |
|---|---|---|---|
| Q1 | change 두 경로를 1차에 어디까지 | C (둘 다) | 모드 인터페이스·스키마에 직접 영향 |
| Q2 | 비인접 버전 업그레이드 허용? | A (인접만, 나머지 STOP) | 미답 시 임시로 STOP 구현 가능하나 답이 정석 |
| Q4 | owner 미정 처리 | B (unresolved + STOP) | **설계 전반이 이미 B 전제** — A 선택 시 설계 수정 필요 |
| Q5 | 배포 방식 입력 | B (매 run 필수 입력) | 위와 동일 — B가 전제 |

미뤄도 되는 5개: Q3(matcher 값 기반 전환 — 1차는 delta만), Q6(승인자 신원 연동 — 운영 전),
Q7(patch-kill — /om-verify 설계 시), Q8(conflict-rate 산정 — om-plan 입력 아님), Q9(exit 2 CI 처리 — wrapper 시점).

## 구현 중 처리할 잔여 주의점 (2차 재검토 신규 P2)

| # | 내용 | 처리 방향 |
|---|---|---|
| NEW-P2-1 | 2차 지시문의 매니페스트 개수 표기(101) vs 실제(102) | 재패키징 시 수치 갱신 |
| NEW-P2-2 | **session marker의 세션 식별 미정** — Claude 창 2개 병행 시 마커 공유 충돌, 한쪽 cleanup이 다른 쪽 보호 해제 가능 | 마커에 세션 ID 포함 or "동시 plan 세션 1개 제한" + 병행 반례 추가 |
| NEW-P2-3 | input-lock의 canonical 필드 규칙 미정 — 재계산 대조의 결정론 보장 필요 | 기존 `verdict.canonical_digest` + canonical/observational 분리 패턴 재사용, 스키마 정의 때 고정 |
| NEW-P2-4 | 01A 출력 표에 `official-doc-snapshots/` 행 누락(트리엔 있음) | 표기 정리 |

## 1차 검토 때 정한 P2 8건 (구현 중 정리 목록, `03` §1 P2)

tool-events 생성 주체(PostToolUse or 1차 제외) · exit 2 CI wrapper · 모순 상태(반영됨) ·
upgrade 이동 정보 중복 제거 · 후속 명령 "(candidate)" 표기 · initial 기준 SHA 세대 구분
(d952a838=111경로 스냅샷 / 8ac18ad0=112경로 활성 기준선) · checker catalog digest 산출 규칙 ·
package manifest 재생성.

## 패키지 자체의 한계 (양 차수 공통 고지)

패키지가 참조하지만 포함하지 않은 코드 — `gitprim.py`·`layout.py`·`binding.py`,
om_workflow가 부르는 실행 스크립트 8종, `commit-inventory.schema.json` —
이 부분의 설계 주장은 코드 대조 없이 문서만으로 판정했다. 구현 검토 때는 원본 저장소에서 직접 확인할 것.

## 구현 착수 순서 (2차 결과 §8 확정)

1. Schema 4종 (run-request → input-lock(canonical 분리) → discovered-facts → validation-attempt/plan-result)
2. 공통 preflight collector (+R01·C39)
3. **plan-validate** (재수집·대조·역참조·잔여 스캔·plan_digest·마커 정리) — Hook보다 먼저 (최후 안전망이므로)
4. 2계층 마커 Hook (+C30·C41·C42·병행 세션 반례)
5. upgrade 문서 수집기 (raw bytes·버전 token)
6. 모드별 수집 확장 → Skill·reference 설치는 운영 도입 조건 충족 후 마지막
