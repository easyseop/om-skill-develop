# Codex 지시 — 2차 검토 P0 2건 수정 (검사기 엔진 + 실행 래퍼)

작성일: 2026-08-25. 배경: 85의 P0-1·P0-2를 Claude가 재검증으로 확정, 사람 결정 완료(24): **feature 신규 ID는 사람이 요청서에 필수 기재**. 이 지시서는 그 결정의 구현이다. 대상: clean-export 작업트리(브랜치 `codex/om-plan-verified-gates-20260820`, **커밋·push·MR 금지** — Claude가 검증 후 대행).

## 작업 1 — P0-1: feature ID 필수화 + 중복 검사 실효화

1. `harness/acgh/plancore/schema/plan-run-request.schema.json`의 feature 절에 `customization_id` **필수** 추가(형식 제약도: 기존 ID 패턴 규칙이 있으면 재사용, 없으면 `^BANK-OM-[0-9]{3}$` 제안하되 기존 등록 ID 실물과 정합 확인 후 결정·기록).
2. 기존 중복 검사(collectors.py:550-551)가 필수화로 실효되는지 확인 — 생략 경로가 스키마에서 먼저 차단되므로 검사 코드 변경은 최소화. 단 **방어 이중화**: `customization_id`가 None/빈문자면 검사기에서도 명시 거부(스키마 우회 대비).
3. **반례 테스트 추가**(85의 실측 반례 재현): ① ID 생략 feature 요청 → 스키마 거부(analysis_error) ② 기존 ID 기재 feature 요청 → "already exists" block ③ **제안서의 affected ID가 요청 ID와 다르거나 기존 ID를 포함**하는 경우의 거동 실측·차단 확인(85 반례는 제안측 재사용이었음 — 요청 ID 필수화만으로 제안측 재사용이 막히는지 검증하고, 안 막히면 제안 affected ID 대조를 보강).
4. 정상 4모드가 거짓 block되지 않는지 기존 전체 테스트 + 리허설급 스팟 확인.

## 작업 2 — P0-2: 실행 래퍼 통일

1. **계약(고정)**: `harness/om` 실행 스크립트(셸) — 사용법 `harness/om <이하 om_workflow.py 인수 그대로>`. 동작: `.venv/bin/python`(저장소 내부, 있으면 우선) → `python3.11`+ → 그 외 명시 오류(종료코드·메시지에 "Python 3.11+ 필요" 안내). `python`/`python3` PATH 의존 금지.
2. **훅·권한 정합(필수)**: `hook_policy.decide_pre_tool_use`의 plan 계열 명령 인식과 `.claude/settings.json`의 allow/deny 패턴이 래퍼 호출 형태(`harness/om plan …`)를 정확히 인식·통제하도록 수정 + 그 반례(래퍼로 위장한 비허용 명령 차단) 테스트. 훅이 안 거치는 직접 python 호출도 기존대로 동작(하위호환).
3. 저장소 문서(README·CLAUDE.md)의 실행 안내를 래퍼 기준으로 갱신.

## 작업 3 — 검증

- 전체 테스트 green(기존 259+ + 신규 반례). 반례가 **실제로 차단**되는 것을 결과서에 실행 출력으로 증명.
- 스킬 파일은 이 지시서 범위 밖(명령 교체·재감사는 Claude가 스킬저장소에서 수행 후, 이식은 다음 지시서).

## 결과

`skill_develop/om_plan/87_Codex_P0수정_결과_20260825.md`: [스키마·코드 변경 위치 / ID 패턴 결정과 근거 / 반례 3종 실행 출력 / 래퍼 계약 구현·훅 정합 근거 / 테스트 집계]. 이후 Claude 검증 → 스킬 명령 교체·확인 감사 → 84 작업 2 재수행(이식) 순서.
