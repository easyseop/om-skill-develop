# Claude 검증 — P0 수정(87) 재검증 + 스킬 반영

검증일: 2026-08-25. 방식: 보고를 믿지 않고 전 항목 재실행·라이브 재현.

## 1. 87 재검증 — 통과

- **변경 범위**: `git status` 10개 파일 = 보고와 정확히 일치(그 외 변경 0).
- **전체 테스트 내 재실행**: 276 passed·fail 0·exit 0. P0-1 반례 지정 재실행 16 pass, 래퍼·wiring 지정 재실행 9 pass.
- **P0-1 라이브 재현**: feature 요청에서 `customization_id` 생략 → `SCHEMA_INVALID`(customization_id 지목) 실측. ID 기재 시 스키마 통과(중복·정책은 다음 층 — 테스트로 확인). 제안서 바꿔치기·기존 혼입 block은 반례 테스트로 확인.
- **래퍼 실물 검토**: `.venv` 우선(버전 미달 시 우회 없이 오류)·명시 버전(3.14→3.11)만 탐색·일반 `python`/`python3` 미사용·미충족 시 exit 2+안내 — 87 계약과 일치. 이 환경(python3.11)에서 실동작 확인.
- **훅 판정 5종 라이브 재현**: 실제 marker 생성 후 `decide_pre_tool_use` 호출 — `harness/om plan` 허용 / 직접 python 하위호환 허용 / `verify` 위장·`echo` 위장·`&& git push` 전부 거부. **전건 보고와 일치**(중간에 2건 불일치로 보였던 것은 검증 스크립트의 판독 실수 — HookDecision.allowed 필드 확인으로 해소, 결함 아님).

## 2. 스킬 반영 (Claude, 스킬저장소)

- 두 SKILL.md의 실행 명령·allowed-tools를 `harness/om`으로 교체(om-plan 6곳·om-verify 3곳), om-plan에 **P0-1 입력 계약 반영**(feature `customization_id` 필수·ID 형식 `^BANK-OM-[0-9]{3,}$`·제안서 바꿔치기 block 사유), om-verify 종료코드 2 설명에 실행기 중단 경우 추가.
- RULE_COVERAGE 앵커 4건 동반 갱신, build 검증 2종 exit 0, AUTHORING_REVIEW에 수정 이력 기록.
- **확인 감사(새 독립 인스턴스) 진행** — 결과에 따라 FINAL_STATUS 갱신.

## 3. 다음

확인 감사 PASS 시: Codex에 84 작업 2 재개 지시(이식·om-apply 한글화(`harness/om` 명령)·wiring 한글 단언·반례·전체 테스트) → Claude 최종 검증 → 커밋분리·push → 실기동 스모크 + **스킬 경유 파일럿 1회**(BANK-OM-005 데이터, "우리 스킬로 실제 운전" 증명 — 사람 결정 반영).
