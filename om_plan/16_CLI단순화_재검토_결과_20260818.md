# CLI 단순화 재검토 결과 (2026-08-18)

- 대상: `codex/om-plan-cli-simplify-20260818` · 범위 `648b3ca..dfdc0bc` (보완 1커밋 "fix: preserve clean repeated plan runs")
- 변경 4파일(코드·테스트만). 방법: diff 정독 + 전체 시험 실행 + **독립 실증 3종**(2연속 run·교차세션 6우회·P2-1 hook 전달)

## 결론: **통과** (남은 것은 P2-1 하나, 비차단)

15번의 P1-1이 해소됐고 P2-2도 완결. 회귀 없음. 남은 P2-1은 15번에서 이미
"기반만 추가, hook 전달 미완결"로 예고한 범위 안이며 안전을 약화하지 않는다.

## 필수 확인 1~5

1. **P1-1 해소 — 실증 통과.** 지난번 RUN-2에서 죽던 probe를 그대로 재실행:
   RUN-1·RUN-2 둘 다 `approval`. 완전 실증(`full_probe`)에서 4조건 전부 참 —
   ① 기본 옵션 start→check 2연속 ② 두 실행 모두 approval ③ 첫 run 보존(`runs[0].is_dir()`)
   ④ checker 작업트리 clean 유지(start·check 후 `git status --short`=="").
   근거: `om_workflow.py:86-102` `default_plan_evidence_root`(→`.git/om-plan-evidence`),
   `:234` 기본 evidence를 이 함수로 교체. `.git` 내부라 porcelain에 안 잡혀 dirty·RUN_DIRECTORY_EXISTS 동시 회피.
2. **증거 위치 이동이 규칙 약화 없음 — 확인.** run-dir·마커·지문 로직 불변, 위치만 `.git/` 하위로.
   보존 규칙: 새 타임스탬프 할당(`allocate_plan_run_dir`)+`RUN_DIRECTORY_EXISTS`로 재사용 여전히 불가.
   재검증: `check_plan_run`이 세션 마커의 신뢰 지문을 그대로 사용(E안 불변). 실증에서 verdict=approval·binding verified.
3. **P2-2 교차세션 차단 — 우회 없음(적대 실증).** 세션 a 마커로 남의 run(rb)을
   6가지 표기(절대경로·`--state-root=`·옵션뒤·`--`·상대경로)로 검증 시도 → **전부 거부**,
   자기 run과 인자없음 자동선택만 허용. 근거: `hook_policy.py:75-108` `_plan_check_run_dir`가
   값옵션(`--x` / `--x=`)을 소비하고 첫 위치인자만 run으로 추출, `:170-183`에서 현재 run과 불일치 시 거부.
4. **P2-1 판단 정확 — 확인.** `om_workflow.py:166` `select_plan_session_id`가 `OM_PLAN_SESSION_ID` env를
   수용(수용 기반 O). 그러나 `hook_cli.py`·`hook_policy.py` 어디서도 이 env를 **설정하지 않음** →
   hook 세션이 자동 start에 전달되는 경로는 미완결. 15번 판단과 일치. (실사용 시 사람이 env 지정하면 동작)
5. **회귀 없음 — 확인.** 레거시 `plan --repo --version`·`plan-preflight/validate` 경로·판정·기존 반례 diff에서
   삭제/변경 없음(추가만). 전체 시험 통과(skip은 타 환경 미러 경로 전용, 신규 아님).

## 남은 것

- **P2-1**(비차단): hook adapter가 `OM_PLAN_SESSION_ID`를 설정해 자동 start에 세션을 물려주면 완결.
  안전 약화 아님(미설정 시 자동 uuid로 fail-closed 유지). 이번 merge 차단 사유 아님 — 후속 작업으로.
- P2-3(skip 사유 고정)은 15번에서 지적, 여전히 열림(경미).
