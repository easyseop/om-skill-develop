# initial 첫 실전 결과 (2026-08-19) — 로컬 CLI, 슬래시 명령 전 기준선

## 목적
로컬 CLI(plan start)로 om-plan을 실제 처음 돌려, ① 도구가 되나 ② 결과가 기존 정답지와
맞나 ③ MD에 담을 재료 수집. 나중에 슬래시 명령 실전과 대조할 기준선.

## 실행 조건
- 검사기: kb-datacatalog-upgrade-checker clone @ 2688e675
- 제품: ~/om-work/OpenMetadata-1.13.1-full (full, 커밋 9개·꼬리표 7개)
- 요청: mode=initial, official=afcb2d2, current_custom=8ac18ad, product_version=1.13.1, owner=null
- 정답지: 기존 매니페스트 7개(d952a838 기준, changed_path_count=111)

## 실행 경위 (막힌 지점 = MD 재료)
1. 1차: ACTIVE_REGISTRATION_EXISTS — 관리 파일이 이미 있어 멈춤(initial은 덮어쓰지 않음). **정상.**
2. 등록자료를 mv로 옮김 → WORKTREE_DIRTY(삭제도 dirty로 감지). **정상 — 검사 중 저장소 무결 요구.**
3. 정공법: 등록자료 제거를 커밋해 clean 확보 → initial 정상 실행.

## 채점 (만점)
| 항목 | 정답지 | initial 발견 | 판정 |
|---|---|---|---|
| 커스터마이징 | 7 (BANK-OM-001~007) | 7 (동일) | ✅ |
| 커밋 | 9 | 9 | ✅ |
| 007 series(2커밋) | series | 9커밋 내 007 2회 인식 | ✅ |
| 변경 경로 | 111(d952) | 112(8ac18ad) | ✅ 1개차=SHA차, 도구정상 |
| 기존 등록 대조 | — | registered=0(빈손) | ✅ |
| 사람 승인 | — | intent_review_required=true, 멈춤 | ✅ |
| run 위치 | — | .git/om-plan-evidence/ (간편화 반영) | ✅ |

## MD에 담을 것 (사용법 초안)
- initial은 관리 파일이 **없을 때만**. 있으면 ACTIVE_REGISTRATION_EXISTS → change/upgrade로.
- 시작 전 두 저장소 **git clean** 필수(WORKTREE_DIRTY).
- 요청 파일 필수: schema_version, run_id, mode, repositories{product,checker}, refs{official,current_custom}.
- 결과는 green/red 아니라 **intent_summary + next_command**로 읽는다. approval=사람 검토 준비.
- owner 미정은 null → owner-status에 unresolved로 남음(자동 추측 안 함, Q4대로).

## 미결/주의
- input-lock의 신뢰 지문을 사람이 보관 → plan check에서 대조(E안). 이번엔 check까지 안 감(start만).
- 정밀 채점(정답지 d952 기준 맞추기)은 필요 시 별도. 이번 목적(발견 정확성)엔 8ac18ad로 충분.
- 다음: 슬래시 명령 구현 후 같은 요청으로 돌려 이 기준선과 대조.
