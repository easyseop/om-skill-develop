# Codex 전달 — /om-plan 전체 인수인계를 om-skill-develop에 커밋

> 작성: 2026-08-17. 이 문서를 코덱스에게 그대로 전달한다.
> 성격: **문서 1개 추가 커밋.** 코드·CI·설정은 건드리지 않는다.

## 1. 할 일

Claude가 작성한 `/om-plan` 전체 인수인계 문서를 **히스토리 정본 저장소 `om-skill-develop`**에
새 번호 문서로 추가하고 커밋한다(push는 사용자 지시 시).

- 원본 내용: `~/Desktop/om-plan_전체_인수인계_20260817.md`
  (읽을 수 없으면 사용자가 내용을 붙여준다.)
- 커밋 위치: `om_plan/10_전체_인수인계_GitLab이전_20260817.md`
  (기존 `00`~`09` 다음 번호. 파일명은 규약에 맞게 조정 가능.)
- 커밋 메시지 예: `docs: /om-plan 전체 인수인계(결정·경위·GitLab 이전 상태)`
- 공동저자 트레일러: `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`

## 2. 반영할 것 (내용 요지 — 원본과 일치시켜라)

원본 문서가 이미 규약("문제→논의→결정→고려→반영→미결") 형식으로 아래를 담고 있다.
누락·상충이 있으면 원본을 우선하고, 코덱스가 아는 최신 사실과 다르면 **지적만** 하라(임의 수정 금지).

- 확정 결정: **Q1=C, Q2=A, Q4=B, Q5=B**, 저장소 경계 **D안**(openmetadata-test 내
  `plancore`+`integrations/om`, 별도 저장소 B안 기각), **GitHub 미사용→GitLab**, **지문 보관=보호 CI**.
- 보류 결정: **Q3·Q6·Q7·Q8·Q9**(특히 Q9 exit 2 취급). 임의 확정 금지.
- 작업 경위 9단계: 3차 검토→P1-A→E안→E-hardening(`1f02e1d3`)→GitHub CI(`9bc4cd2c`)→
  `.pyc` 수정(`7aaa18f`)→GitLab 전환→GitLab 배선(`ee9f033`)→제품 clone blob 수정(`ef48a8e`).
- 현재 상태: 검사기 최신·채택 커밋 **`ef48a8e`**(branch `codex/om-plan-gitlab-product-clone-20260817`),
  제품 1.13.1 완전본 `~/om-work/OpenMetadata-1.13.1-full`(전체 이력 누락 0).
- 남은 작업: GitLab push(저장소 2개)→외부 설정(protected env·runner·branch·변수)→실 pipeline 1회.
  `/om-implement`·`/om-verify`는 미착수.

## 3. 하지 말 것

- 코드·Schema·test·`.gitlab-ci.yml`·활성 등록자료 변경 금지. **문서 1개 추가만.**
- 보류 Q3·Q6·Q7·Q8·Q9 임의 확정 금지.
- push·merge·release는 **사용자 지시 시에만.** 기본은 커밋까지.
- 원본과 다른 사실을 발견하면 문서를 몰래 고치지 말고 **차이를 보고**하라.

## 4. 종료 조건

- `om_plan/10_...md` 커밋 완료(커밋 SHA 보고).
- 원본과의 차이/최신 사실 불일치가 있으면 목록으로 보고.
- 이후 GitLab 이전(저장소 2개 push)과 외부 설정은 사용자 승인으로 진행한다.
