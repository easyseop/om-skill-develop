# CLI 단순화 적대적 검토 결과 (2026-08-18)

- 대상: `work/review-openmetadata-test-om-plan-cli` · 브랜치 `codex/om-plan-cli-simplify-20260818`
- 기준 `2688e675` ⊂ 구현 `648b3ca` 확인. 변경 5파일(코드·테스트만 — 문서 규율 준수 ✓)
- 방법: diff 전문 정독 + 전체 테스트 실행(통과, skip 6건) + **픽스처 재사용 2연속 run 실증**

## 결론: **P1 1건 수정 후 재검토** (merge 보류)

요구사항은 사실상 전부 충족했고 설계 품질도 높다(아래 ✓ 목록). 그러나 기능의 존재
이유인 "반복 사용 간편화"가 **기본 설정에서 2회차에 죽는** 결함이 실증됐다.

## P1-1 — 기본 경로에서 두 번째 `plan start`가 항상 거부됨 (실증)

| | |
|---|---|
| 근거 | `om_workflow.py` `start_plan_run`: 기본 `evidence_root = project_root/"evidence"`(저장소 안) · `preflight.py:283-292` `WORKTREE_DIRTY`(완전 무결 요구, 이전 run 제외 없음) · `.gitignore`에 `evidence/` 없음 |
| 실증 | 픽스처 재사용 probe: RUN-1 `approval` ✓ → RUN-2 **start 거부** `"planning starts only from clean repositories and never cleans them"` — 보존된 RUN-1 폴더가 dirty로 잡힘 |
| 충돌 | "완료 run 보존" 규칙과 정면 충돌 — 재사용하려면 이전 run을 지우거나(보존 위반) 커밋해야 함 |
| 참고 | 잠재 결함은 기준 커밋부터 있었으나(레거시도 repo 안 run-dir이면 동일), **이번 기본값이 이를 기본 동작으로 만듦** |
| 수정 권고 | `.gitignore`에 `/evidence/` 추가(porcelain은 ignored 제외 → preflight·validate 둘 다 해소) **또는** 기본 evidence root를 state-root처럼 `git rev-parse --git-path om-plan-evidence`로. 어느 쪽이든 **반례 추가: "기본 경로로 start→check 2연속 모두 approval"** |

## P2 (비차단, 기록)

| # | 내용 | 근거 | 권고 |
|---|---|---|---|
| P2-1 | **hook 컨텍스트에서 자동 session-id가 막다른 길** — hook은 이벤트 session("claude-session-a")으로 마커를 만드는데, `plan start`가 session-id 미지정 시 `local-uuid`를 새로 만들어 run이 다른 마커에 결속 → hook 세션은 영영 unbound → 이후 proposal 쓰기·plan check 전부 거부(fail-closed라 우회는 아님) | `test_plan_workflow.py:1202` base session vs `om_workflow._new_plan_session_id` | hook adapter가 환경변수(예: `OM_PLAN_SESSION_ID`)를 설정하고 start가 미지정 시 이를 우선 사용 |
| P2-2 | **교차 세션 검증 허용** — hook은 `plan check <다른 세션의 run_dir>`를 허용(자기 run_pair 존재만 확인), 검증 완료 시 상대 세션 마커까지 정리됨 | `hook_policy.py:133-138` | hook에서 plan check의 run_dir 인자가 자기 run과 일치하는지 확인(또는 보호 중엔 인자 없는 자동 선택만 허용) |
| P2-3 | 전체 스위트 skip 6건의 사유·기준 커밋 대비 신규 여부 미기록 | 실행 로그 | `-rs`로 사유 고정 후 기준과 대조 |

## 요구사항 충족 확인 (전부 코드·테스트로 검증)

- `plan start <요청>` 한 명령: run-dir 자동(재사용 불가 — 할당기+`RUN_DIRECTORY_EXISTS` 이중) · state 기본값이 **`.git/om-plan-state`**(작업트리를 더럽히지 않는 영리한 선택) · session 자동+충돌 재시도 ✓
- `plan check`: 단일 활성 run 자동 선택, **2개 이상이면 목록 내고 중단**(`PLAN_RUN_AMBIGUOUS`) · run↔마커 소유 교차검증(`MARKER_OWNERSHIP_MISMATCH`) ✓
- **E안 보존 강화**: preflight 지문을 **세션 마커(신뢰 채널)에 보관**, check가 그걸 기대값으로 사용. 다른 값 공급 시 거부. 마커에 없고 외부 공급도 없으면 fail-closed. **P1-A 3파일 재작성 회귀 테스트 포함** ✓
- Hook 경계: `plan start/check` 토큰 인식, run 활성 중 두 번째 start 거부, 임의 Bash는 default-deny 유지 ✓
- 레거시 호환: 기존 `plan --repo --version`·`plan-preflight/validate` 경로 불변, 미지정 시 친절한 오류 ✓
- 반례 ①②③④ 전부 실재하며 monkeypatch 충돌·digest 변조 거부 등 **진짜 반례** ✓ · 문서 md 0건 ✓
